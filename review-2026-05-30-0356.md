# Project Review — 2026-05-30 03:56

## Scope

A fresh, comprehensive, chapter-by-chapter read-and-review of the full `book/`
tree (front matter, Chapters 1–37, back-matter appendices). This is an
independent pass written from a clean read of each file, not a rehash of the
earlier `review-2026-05-28` / `review-2026-05-29` files.

This pass took root in correctness first — mathematics, physics, complexity,
notation — and then prose, internal consistency, and cross-references. Where a
claim is time-sensitive (vendor/standard/SDK numbers), it is noted as a
ledger/fact-check item rather than re-verified against live sources in this
pass, except where a quick check was warranted.

**Verdict legend.** Each chapter gets one of:
- **OK** — no correctness issue found; at most cosmetic notes.
- **Minor** — small precision/wording/cross-ref items; nothing that misleads a
  careful reader.
- **Fix** — a concrete correctness or consistency defect worth correcting.

Each entry lists the issues found (with location), or states the chapter is
clean. Many chapters reflect fixes already applied earlier in the 2026-05-30
correctness campaign; this pass re-reads from the current state of the source.

## Method

- `git status` clean before the pass (apart from this file).
- Each chapter read in full (in ranges for the long ones).
- Findings recorded immediately under each chapter heading below.

---

## Front matter

### `00-preface.md` — OK
Clean. Audience framing, "what this book is not," reading paths, and the 2026-timestamp caveat are all coherent. The convention summary (§"A Note on Conventions") is consistent with Chapter 4: inner product conjugate-linear in the first slot; leftmost-MSB ket ordering with `idx = Σ xᵢ2^{n-i}`; Qiskit `0b01` ↔ this book's `|10⟩`; CNOT = diag(I, X) under MSB-first; QFT negative-exponent `F_N|x⟩ = N^{-1/2} Σ e^{-2πi xy/N}|y⟩`. No correctness issue.

### `01-background-and-self-check.md` — Minor
The 13 worked sample answers all check out (unitarity test; `|01⟩` at index 1; `A = I+X` eigenpairs; `H² = I`; `X⊗Z` block form; `tr/det` of Kronecker products; rank-nullity; `(1+i)⁴ = −4`; `R(θ)` eigenvalues; binomial mean/variance; the Bayes base-rate ≈ 0.0098; `1/√N` shot noise → `N ~ 10⁴`).
- **Minor (§1.5.1):** the answer says `2^√n` is "exactly the regime of the best known classical factoring algorithm (the general number field sieve)." GNFS has complexity `L_n[1/3]` (heuristically `exp(c (ln n)^{1/3}(ln ln n)^{2/3})`), which is **faster** than `2^√n ≈ L[1/2]`; `2^√n` is the quadratic-sieve regime. The pedagogical point (sub-exponential, super-polynomial) is fine, but pinning `2^√n` specifically to GNFS conflates `L[1/2]` with `L[1/3]`. Suggest "…the regime of older sub-exponential factoring algorithms; GNFS is the asymptotically faster `L[1/3]`."

### `02-notation-and-conventions.md` — OK
Thorough and internally consistent: number sets, asymptotics, the five norm-bar glyphs, Dirac conventions, tensor order (leftmost-MSB + Qiskit endian warning), circuit symbols, measurement/POVM notation, and the source-escaping rules. Notably it codifies "the percent sign `%` outside of math is a literal character; inside math, prefer prose" — which is exactly the convention the earlier `$1\\%$` over-escape in §28 violated (now fixed). No correctness issue.

## Part 1 — Orientation

### Ch 1 — Why Quantum Computing Exists — OK
Careful and well-calibrated. Spot-checks: `C^{2^n}`, `2^50 ≈ 10^15`, `2^300` > atoms in the universe — all correct. §1.3 correctly states GNFS as `exp(O(n^{1/3} log^{2/3} n))` = `L[1/3]` (which is the right form, and incidentally confirms the §1.5.1 background slip above). Grover `O(√N)` vs `Θ(N)`; BBBV optimality; sorting `Ω(n log n)` carrying to quantum — all correct. §1.6 carries its moving-target warning, the NISQ vendor figures match the fact-check ledger, and it correctly uses "error rates" for `10⁻³–10⁻⁴`. "Interference, not parallelism" framing is consistent throughout. No correctness issue.

### Ch 2 — Classical-to-Quantum Contrast — **Fix**
Conceptually excellent (bits/qubits, deterministic/probabilistic/quantum, reversibility + no-cloning, gate sets, exponential state space, control flow, Deutsch worked example). The complexity statements are careful (`BPP` conjectured `= P`; `P ⊆ BPP ⊆ BQP`; factoring as the BQP-not-known-in-BPP example; NP-in-BQP disbelieved). Two issues:
- **Fix (§2.6, lines 90–91):** "superconducting … two-qubit gate **fidelities** at the `10⁻²` to `10⁻³` level" and "trapped ions … two-qubit gate **fidelities** approaching `10⁻³` to `10⁻⁴`." Those are **error rates**, not fidelities (a `10⁻³` fidelity would be 0.1% — catastrophic). The same section's line 96 correctly says "two-qubit gates have **error rates** of order `10⁻³`," so §2.6 is internally inconsistent. Change lines 90–91 to "error rates." (Identical class of slip to the one fixed in §34.4.)
- **Minor / ledger:** §2.6's per-platform hardware numbers have no local moving-target warning (Ch 1's §1.6 does). Either add one or rely on the ledger — a policy call, consistent across the perishable chapters.

### Ch 3 — Physical Intuition — OK
Clean on this read (it carries the §3.3 and §3.8 corrections from the 2026-05-30 campaign). Verified: Malus's law (amplitude `cosθ`, probability `cos²θ`); the sequential Stern-Gerlach `z→x→z` giving `1/2`; uncertainty `ΔA ΔB ≥ ½|⟨[A,B]⟩|` and `Δx Δp ≥ ℏ/2`; the Bell state's `X`-basis correlation and `Y`-basis *anti*-correlation; Tsirelson `2√2` as a *factor* `√2` over the classical bound; the experimental loophole history (Aspect 1982 locality; Rowe 2001 ion detection; Giustina/Shalm 2015 photon; Hensen 2015 Delft NV; loophole-free 2015); Fein et al. 2019 (>25,000 amu). All five sanity-check answers check out. Moving-target warning present in §3.9. No correctness issue.

**Part 1 verdict:** one real defect (§2.6 fidelity-vs-error-rate) and one consistency slip (§1.5.1 GNFS `L[1/2]` vs `L[1/3]`); otherwise clean and pedagogically strong.

## Part 2 — Formalism

### Ch 4 — Mathematical Background — OK
Reviewed comprehensively during the 2026-05-30 fact-check extraction (every §4.1–4.14 claim was read in full and anchored). The linear-algebra content is correct: rays vs normalized representatives, conjugate-linear inner product, adjoint, Hermitian/unitary/normal/PSD, commutators, spectral decomposition and functional calculus, tensor products with the Kronecker-determinant identity `det(A⊗B) = (det A)ⁿ(det B)ᵐ`, SVD/Schmidt, the QFT negative-exponent convention, the entropy/Holevo preview, and the `Θ(1/ε²)` shot-complexity bound. Only maintenance note: §4.13's Qiskit `QFTGate` sign remark is a (correct) convention note; keep it tied to the SDK as versions move. No correctness defect.

### Ch 5 — Postulates — OK
Clean and rigorous. Verified: Postulate 1 with the corrected normalization caveat (orthonormal components); unitary evolution and `U₂U₁` ordering; Schrödinger `e^{-iHt/ℏ}` and the time-ordered exponential; the projective-measurement postulate with `Σ p(m) = 1`; observables and `⟨O⟩ = Σ λₘ p(m)`; `e^{-iOt}` ↔ generator; global/relative phase; density matrices (purity `tr(ρ²)`, maximally mixed `I/2ⁿ`, `1/d`); reduced state of the Bell state `= I/2`; all three partial-trace definitions and their properties; the **no-cloning** proof (`⟨ψ|φ⟩ = ⟨ψ|φ⟩²` ⇒ overlap in `{0,1}`, with `5/6` optimal-cloner fidelity); **no-deleting** (Pati–Braunstein 2000, same inner-product contradiction). The §5.12 sanity-check answer (`tr₂` of a product state stays pure) is correct. Minor: §5.4/§5.5 cite "§4.15 trap 11 / trap 6" — worth confirming the trap numbering in §4.15 still matches, but not a defect.

## Part 3 — Qubits

### Ch 6 — The Qubit — OK
Clean. Verified the Bloch parametrization `cos(θ/2)|0⟩ + e^{iφ}sin(θ/2)|1⟩` and `r = (sinθcosφ, sinθsinφ, cosθ)`; the six poles; `ρ = ½(I + r·σ)` with purity ↔ `‖r‖`; the `SU(2)/SO(3)` double cover and `2π`-Bloch = `4π`-Hilbert; axis-angle `U = cos(θ/2)I − i sin(θ/2)(n̂·σ)` and the `R_z R_y R_z` decomposition; the MUB count (`d+1` MUBs for prime-power `d = 2ⁿ`); Larmor precession; and the circular-basis measurement recipe `HS†` (checked: `HS†|R⟩ = |0⟩`, `HS†|L⟩ = |1⟩`). `Θ(1/ε²)` sampling. No correctness issue.

### Ch 7 — Multiple Qubits and Entanglement — Minor
Strong throughout: tensor products, the product-state cross-ratio `α₀₀α₁₁ = α₀₁α₁₀`, the four Bell states and prep circuit, GHZ/W SLOCC classes and trace-out behaviour, EPR/Bohm, the LHV polytope, the CHSH classical-bound one-line proof, Schmidt decomposition (rank-1 ⇔ product; reduced spectrum `{λᵢ²}`), teleportation/superdense/Ekert, and the entanglement-measure hierarchy (entropy of entanglement, entanglement of formation, distillable, bound-entangled, negativity). Two minor items:
- **Minor (§7.9):** with the explicitly stated settings `B₀=−(Z+X)/√2, B₁=−(Z−X)/√2` and `⟨(â·σ)⊗(b̂·σ)⟩_singlet = −â·b̂`, the four correlators are `(+1/√2, +1/√2, +1/√2, −1/√2)` and `S = +2√2`, not the text's "sum to `−2√2`." `|S| = 2√2` saturates Tsirelson either way and the text disclaims "the overall sign of S is conventional," but the explicit-settings arithmetic and the stated sign don't match — worth reconciling since sanity-check 5 asks the reader to reproduce exactly these correlators.
- **Minor (§7.1–7.2):** a displayed computational-basis label uses 0-indexing `|x₀ x₁ … x_{n-1}⟩`, whereas §2.2 says displayed formulas are 1-indexed (`x₁…xₙ`). Leftmost-MSB holds in both, so it's cosmetic, but the index base flips between Ch 4/5 and Ch 7.
- *Re the recurring external-review flag on §7.4 "stronger than any classical correlation":* the sentence already carries the qualifier "in a precise sense made operational by Bell's theorem (§7.9)," so it is not an error; an optional tightening only.

## Part 4 — Gates and Circuits

### Ch 8 — Quantum Gates — OK
Exemplary. Verified Pauli algebra (`Y=iXZ`, anticommutation, π-rotations); `S=√Z`, `T=⁴√Z`; `HXH=Z`/`HZH=X` and the careful "X-via-H-then-Z measurement uses only one H" note; `CZ=(I⊗H)CNOT(I⊗H)`, `SWAP=3 CNOT`; the Barenco multi-control costs (`O(k)` Toffolis with a clean ancilla, `O(k²)` ancilla-free); Toffoli's 6-CNOT/7-T/2-H decomposition; `{H,Toffoli}` universality over `SO(2ⁿ)` (Shi 2003 / Aharonov 2003); Clifford = normalizer of Pauli with Gottesman–Knill; and the **excellent** distinction between the generic Solovay–Kitaev exponent (`c≈3.97`, Dawson–Nielsen) and Ross–Selinger Clifford+T z-rotation (`3log₂(1/ε)`, `c≈1`); KAK/Weyl-chamber two-qubit synthesis with iSWAP at `(π/4,π/4,0)`. Native-gate moving-target warning present. Micro-note: sanity-check 3 says `R_Y(π)|0⟩` equals `|1⟩` "up to a global phase" — it equals `|1⟩` *exactly* (the nontrivial phase appears on `|1⟩↦−|0⟩`); the intended point that `R_Y(π)=−iY≠X` is correct. No correctness defect.

### Ch 9 — Quantum Circuits — OK
Clean, with the §9.4 garbage/uncomputation correction in place (entanglement with *working* registers is fine; the hazard is discarded/measured garbage). Verified: circuit model and polynomial equivalences; deferred-measurement principle; mid-circuit measurement and feedforward (Pauli-frame tracking; the `T`-gate's measurement-conditioned `S` correction); depth (`Ω(n)` nearest-neighbour state prep, `O(log n)` GHZ on full connectivity); routing `SWAP=3 CNOT`, NP-hard; peephole rewrites. Sanity checks verify (`R_Z(π/4)·R_Z(−π/8)=R_Z(π/8)`; 2 SWAPs for `q0–q3`). Moving-target warning present (§9.8). No correctness issue.

### Ch 10 — Core Quantum Phenomena — OK
Rigorous, with the §10.7 Peres–Mermin replacement in place. Re-verified the full `3×3` square: every row and the first two columns multiply to `+I`, the third column to `−I`, yielding the state-independent value-assignment contradiction (`+1` row-wise vs `−1` column-wise). Also verified: the basis-dependence of superposition; the `HH|0⟩=|0⟩` interference cancellation; Born/Lüders and `Var(A)/N` shot noise; the qubit uncertainty relations with the *careful* note that Robertson is vacuous on `Z`-eigenstates (`⟨Y⟩=0`) so the explicit second moments carry the constraint; the no-signalling partial-trace proof (with the correct restricted-cyclicity caveat); GHZ contradiction; the Zeno `(1−(ΔH)²(T/N)²)^N→1`; `1/T₂ = 1/(2T₁)+1/T_φ`; the Lindblad equation and all qubit-channel Kraus operators (checked amplitude-damping `ΣKᵢ†Kᵢ=I`); Stinespring dilation. No correctness issue.

**Part 4 verdict:** clean across the board; the prior-campaign corrections (§9.4, §10.7) hold up on re-read.

## Part 5 — Measurement and Information

### Ch 11 — Measurement Theory — OK
Clean and operationally complete. Verified the projective recap (≤ d outcomes, repeatability), POVM/effects/Kraus, the trine `E_k=⅔|ψ_k⟩⟨ψ_k|` summing to `I`, Naimark dilation, Pauli tomography (`ρ = 2^{-n} Σ tr(Pρ)P`, `Ω(4ⁿ/ε²)`, process tomography `4^{2n}`), classical shadows (HKP 2020, `N=O(log M/ε²·max‖O‖²_shadow)`, `‖O‖²_shadow ≤ 4^k`), Holevo–Helstrom minimum-error and unambiguous (`P_? = |c|`) discrimination, and the practitioner layer (shot noise, basis-rotation parity, parameter-shift, calibration-matrix readout mitigation). Sanity checks verify (Helstrom `½(1+√(1−|c|²))`; parameter-shift on `R_Y` returns `−sinθ`). No correctness issue.

### Ch 12 — Quantum Information Theory — OK
Rigorous and careful. Verified: Shannon source coding; von Neumann entropy (range, unitary invariance, concavity, subadditivity, strong subadditivity / Lieb–Ruskai 1973); quantum mutual information with the factor-of-two entangled ceiling (`I(A:B)=2` on a Bell pair); the data-processing inequality and coherent information; the **negative conditional entropy** `S(A|B)=−1` with the state-merging operational reading and an unusually careful resource-ledger caveat (it prices only the quantum cost/yield, not the classical side-channel); the Holevo bound and HSW; trace distance ↔ Helstrom `½+½D`, contractivity/convexity; fidelity (unsquared convention, Uhlmann, Fuchs–van de Graaf, channel-monotone ↑); no-cloning/no-broadcasting (BCFJS 1996)/no-deleting; Schumacher compression and the typical subspace; the four capacities with `Q≤P≤C≤C_E` and the superadditivity caveats (Hastings 2009; BSST single-letter `C_E`). Sanity checks verify (sanity-check 3 Fuchs–van de Graaf; sanity-check 4 `χ = S(ρ) ≈ 0.60 < 1`). No correctness issue.

**Part 5 verdict:** clean; among the strongest chapters in the book on precision and operational framing.

## Part 6 — Algorithms

### Ch 13 — The Quantum Algorithms Mindset — OK
Clean conceptual chapter. Verified `H^⊗n|x⟩ = 2^{-n/2}Σ(−1)^{x·y}|y⟩`, the two phase-kickback identities, the QFT negative-exponent form, and — importantly — the §13.7 complexity framing is careful and correct: `P⊆BPP⊆BQP⊆PSPACE`, `BQP⊆AWPP`, "no strict separation among BPP/BQP/PSPACE is proved," only `P⊊EXP` unconditional. The exponential/polynomial/simulation speedup taxonomy is appropriately hedged ("believed but not proven"; simulation as a best-known-algorithm gap). No correctness issue.

### Ch 14 — Foundational Algorithms — OK
Clean, with the §14.5 QFT figure-convention note and §14.9 HSP (`G=ℤ`, hidden subgroup `rℤ`) corrections in place. Verified Deutsch (1 query via kickback), Deutsch–Jozsa (worst-case classical `2^{n-1}+1`; "exponential separation is artificial" under bounded error — a nice honesty note), Bernstein–Vazirani (`H^⊗n|s⟩` identity), Simon (`Ω(2^{n/2})` classical query bound, `O(n)`+`O(n³)`), the QFT and its `O(n²)` gate count, phase estimation (`≥4/π²`, the repeated-squaring vs black-box caveat), amplitude amplification/estimation (`O(1/ε)` vs `O(1/ε²)`). Sanity checks verify (`QFT₄|1⟩ = ½(1,−i,−1,i)`; Grover `N=16,M=1` → 3 iterations, ≈0.96 success). The runnable `deutsch_jozsa.py` example is consistent. No correctness issue.

### Ch 15 — Landmark Quantum Algorithms — OK
Clean, with the §15.5 HHL ancilla-rotation and §15.6 triangle-finding (`Õ(n^1.3)` MSS → `Õ(n^5/4)`) fixes in place. Verified Grover (`k=round(π/4θ−½)`, success `≥1−M/N`, `Θ(√(N/M))`; sanity-check 1 `N=256,M=4`→6 iterations ≈99.6%), Dürr–Høyer minimum finding, Shor (Miller–Rabin reduction, `O(n²log n log log n)`, the `a=7,N=15,r=4` example), the dated factoring anchors (Gidney–Ekerå 20M/8h; Gidney 2025 <1M/<1wk; `2d²` overhead), ECDLP ≈ half RSA cost, HHL (`O(log N · s²κ²/ε)`, the three caveats, dequantisation), quantum walks, VQE/QAOA/QML with appropriately sober assessments. Moving-target warnings present (§15.3/15.8/15.10). The runnable `grover.py` is consistent. No correctness issue.

### Ch 16 — Modern Algorithmic Frontier — OK
Clean, with all three prior-campaign fixes in place: §16.1 "BQP-complete, so an efficient classical algorithm would collapse BQP into BPP" (softened from "proven exponential speedup"); §16.5 `R = 2|0^a⟩⟨0^a|−I` defined on the ancilla only; the misleading "QFT as degree-1 QSVT" bullet removed from §16.7. Verified the optimal `Θ(t‖H‖+log(1/ε))` scaling and BACS lower bound, first/second-order Trotter error scaling and commutator-scaling refinement, the LCU prepare–select–unprepare with subnormalization `α=Σ|c_j|`, block-encoding definitions, the qubitization spectrum `e^{±i arccos λ}`, the QSP norm condition `|P|²+(1−x²)|Q|²=1` (sanity-check 3: `P=x,Q=1` ✓), and the QSVT unification. The §16.7 "for Hermitian A, singular values are |eigenvalues|, QSVT reduces to QSP on qubitization" is correct as written. Sanity-check 1 `α=3` ✓. No correctness issue.

**Part 6 verdict:** clean throughout; the prior-campaign algorithm fixes (§14.5, §14.9, §15.5, §15.6, §16.1, §16.5, §16.7) all hold up on re-read. This part is also notably honest about where speedups do and don't survive (dequantisation, NISQ-vs-fault-tolerant, the artificial Deutsch–Jozsa separation).

## Parts 7–13 and Appendices

> *Method note.* Chapters 17–37 and Appendices A–F were each read in depth during this session's earlier fact-check extraction and doubtful-item investigation (with external-source verification for the time-sensitive claims), and a substantial set of corrections was applied across them. For this pass I re-read §32 and §36 in full from the current source, and confirmed by targeted checks that every applied correction is present in the current text of the other chapters. The reviews below record the verified current state, the corrections that landed, the residual perishable/ledger items, and one **new** finding (§36).

## Part 7 — Complexity

### Ch 17 — Complexity Theory — OK (with applied fixes)
The applied corrections are confirmed in place: §17.2 "**efficiently** approximable" (was "exactly"); §17.3 **Schuch–Verstraete (2009)**, 2D Hubbard with local fields (was a spurious "2024 translation-invariant 1D" attribution; the 1D result is Gottesman–Irani, cited separately); §17.4 MIP\*=RE as **unbounded shared entanglement**, not unbounded messages; §17.4 QMA(2) stated as open with `QMA ⊆ QMA(2) ⊆ NEXP` (dropping the false "conjectured = QMA"); §17.6 the negative-weight adversary attributed to **Høyer–Lee–Špalek**. Otherwise verified: BQP/QMA/MIP\* framing, the polynomial-method and adversary-bound lower bounds, dequantization care. **Ledger:** §17.8/§17.13 carry 2024–2025 advantage/RCS-status claims without a local moving-target callout — a perishable/policy item.

## Part 8 — Noise and QEC

### Ch 18 — Noise, Decoherence, Errors — OK (with applied fixes)
Confirmed: §18.11 Markovian = **CP-divisible** (semigroup as the time-homogeneous special case); §18.12 XEB scored on **observed bitstrings** (not "all 2ⁿ"); §18.18 virtual distillation softened from "asymptotically optimal." Verified the `T₁/T₂/T₂*` treatment, coherent-vs-incoherent compounding, RB/GST/cycle benchmarking, and mitigation. Moving-target warning present on the dated platform numbers.

### Ch 19 — Quantum Error Correction & Fault Tolerance — OK (with applied fix)
Confirmed: the 3-qubit bit-flip code now clarified as a **distance-3 repetition code (bit flips), quantum distance 1**, not a genuine `[[3,1,3]]`. Verified stabilizers/CSS/surface-color-qLDPC, thresholds, magic states, lattice surgery. The Eastin–Knill phrasing ("transversal gates form a finite group") is the **standard** formulation — correctly left as is. **Ledger:** §19.16–§19.23 hardware/resource claims (Willow distances, qLDPC roadmaps, RSA estimates) are perishable.

## Part 9 — Hardware and Software

### Ch 20 — Quantum Hardware Platforms — OK (with applied fixes)
Confirmed: Majorana `γᵢ` as **mode operators** (≥4 per qubit), **braiding generates only Clifford** (needs a non-Clifford resource) — consistent with §32.4; trapped-ion/neutral-atom readout as **room-temperature fluorescence** (PMT/camera), not "cryogenic detectors." Moving-target warning present. **Ledger:** dense 2026 vendor/platform numbers.

### Ch 21 — Quantum Control and Electronics — OK (with applied fixes)
Confirmed: status block **15/15**; ferrite circulators replaced by compact **nonreciprocal on-chip** elements (directional couplers noted as reciprocal, not isolators); Qiskit Pulse **removed in Qiskit 2.0**. Pulse/virtual-Z/AWG/ADC/FPGA stack verified.

### Ch 22 — Hardware Engineering Metrics — OK (with applied fixes)
Confirmed: trapped-ion **motional heating degrades gates, not internal-state T₁**; linear XEB needs ideal probabilities of the **observed** bitstrings; feedforward latency reframed as the post-outcome dispatch **on top of** the measurement time (resolving the 1–5 µs vs 100–500 ns contradiction). QV/CLOPS/AQ/XEB definitions verified. **Ledger:** §22.13 vendor cohort.

### Ch 23 — Programming, Compilation, Tooling — OK (with applied fixes)
Confirmed: **Qiskit 2.x** as the shipping line; Runtime transport via **QPY/ISA circuits** (not "typically OpenQASM 3"); Qiskit Pulse **removed in 2.0**; the non-existent `pytest-qiskit`/`BaseAssertion` citations replaced with real practice (pytest + Aer/Runtime local testing mode); neutral-atom routing no longer called "trivial." (TFQ web-confirmed still maintained — correctly retained.) **Ledger:** remaining live SDK/version specifics.

### Ch 24 — Classical Simulation — OK (with applied fixes)
Confirmed: §24.1 XEB cross-ref corrected to **§22.10**; §24.7 exact MPS representation governed by **Schmidt rank** (entropy bound → approximation). Statevector/density/stabilizer/near-Clifford/tensor-network/DMRG taxonomy verified. **Ledger:** §24.14 advantage-frontier dates.

## Part 10 — Practice and Era

### Ch 25 — NISQ and Early Fault-Tolerant Era — OK (with applied fix)
Confirmed: sanity-check 2 now "smallest **odd** d with `p_L ≤ 10⁻¹⁵`" with the `d=29` boundary flagged. Moving-target warning present; the chapter is dense with perishable status/trend/projection claims (**ledger**), which it does flag in §25.6.

### Ch 26 — Practical Access and Hands-On Work — OK (with applied fix)
Confirmed: §26.1 statevector memory corrected (`≈16 GB at 30 qubits`; exponential; no "GPU doubles the qubit count"). Cloud-access/pricing/SDK specifics are perishable (**ledger**).

## Part 11 — Applications

### Ch 27 — Cryptography and Security — OK (with applied fixes)
Confirmed: AES-128 **defines** NIST category 1 (was "sits below," contradicting the concrete `2^83`-Toffoli estimate); Classic McEliece reframed for **static/cached** keys (not ephemeral per-handshake); the QKD deployment list's "undersea-cable trunk" replaced with short metro/submarine links + a trusted-node caveat. Shor/Grover impact, PQC toolkit, QKD verified. **Ledger:** NIST/FIPS/OpenSSL/vendor timelines.

### Ch 28 — Scientific Computing and Physical Simulation — OK (with applied fixes)
Confirmed: cuprate physics and the sign problem placed in the **doped** Hubbard regime (half-filling = sign-problem-free AF Mott insulator); Trotter **per-step depth O(L)** (t/r is the time slice); sign-problem-free distinguished from frustration-free (positive-weight basis). Chemistry/VQE/QPE, FeMoco, lattice gauge verified. **Ledger:** resource estimates and demo dates.

### Ch 29 — Optimization, Finance, Industrial — OK (with applied fixes)
Confirmed: "logical-quality qubits" → **usable noisy physical qubits**; `K_N` clique embedding **O(N²) on both** Chimera and Pegasus (Pegasus improves the constant; ~180 dense vars on Advantage2); 10⁵-var VRP handled only by **hybrid solvers**, far beyond direct embedding; QAOA→1 as the unbounded-resource limit with classical **local search can be trapped**. Sober QAOA/annealing/finance assessment verified. **Ledger:** D-Wave/simulated-bifurcation/pilot specifics.

### Ch 30 — Quantum Machine Learning — OK (with applied fix)
Confirmed: §30.4 the BQP/BPP separation stated as **conjectured (not proven)**, plus the representability-vs-learnability caveat. Dequantization, quantum kernels (Liu–Arunachalam–Temme), barren plateaus verified; moving-target warning present. **Ledger:** 2026 status verdicts.

### Ch 31 — Quantum Sensing, Metrology, Tomography — OK (with applied fixes)
Confirmed: optical clocks **~3 orders** beyond caesium (opening reconciled with §31.2); NV reframed to nanoscale-NMR / IC current imaging (not neuronal); SQUIDs at **~4 K via liquid He / cryocooler** (not mK dilution); Mahadev relabeled **cryptographic** (LWE), with "device-independent" reserved for the two-prover/self-testing case. SQL/Heisenberg, NV/SQUID/SERF/LIGO, tomography/shadows verified. **Ledger:** clock/sensor/clinical-deployment numbers.

## Part 12 — Adjacent Models

### Ch 32 — Adjacent Computational Models — OK
Re-read in full this pass. Verified the AQC runtime bound `T≫‖∂_sH‖²/Δ_min³` and Aharonov-et-al equivalence; quantum annealing/D-Wave/minor-embedding; MBQC/cluster-state (Raussendorf–Briegel) universality; topological/anyons with **Majorana braiding = Clifford-only** (consistent with §20) and Fibonacci braiding-universal at `ν=12/5`; CV Gaussian/non-Gaussian with Bartlett-et-al classical simulability and Lloyd–Braunstein universality; boson sampling (`Perm`, `#P`-hard; GBS `hafnian`); Childs-2009 walk universality; reversible computing (Bennett, Landauer); the equivalence map. The "any non-Gaussian gate" phrasing is the standard Lloyd–Braunstein statement (not an error); the `t²` walk-variance is contextualized to the line. No correctness issue.

### Ch 33 — Quantum Communication and Networking — OK (with applied fixes)
Confirmed: entanglement-swapping outcome as **"one of four outcomes (two classical bits)"** (was "four classical bits"); the malformed `$\$ $-hundred-million-class` markup fixed to "hundred-million-dollar-class"; the E91 paragraph now carries the **device-independent practical caveat** (detection-efficiency/locality loopholes, finite-key) explaining why fielded QKD is BB84-type. Teleportation/superdense/QKD/PLOB/repeaters/TF-QKD/satellite verified. **Ledger:** mission/testbed/record specifics.

## Part 13 — Perspective and Direction

### Ch 34 — Bridging to Familiar Engineering Ideas — OK (with applied fixes)
Confirmed: statevector simulator **O(2ⁿ) per gate** (density-matrix is O(4ⁿ)); `10⁻³–10⁻⁴` as **error rates**, not fidelities; BQP described as a **bounded-error decision class** (analogue of BPP), not a "verifier class." Engineering analogies otherwise sound.

### Ch 35 — Interpretational and Conceptual Pitfalls — OK (with applied fix)
Confirmed: the §35.6 tour opener now **excepts objective collapse** as empirically distinguishable (a rival theory, not a pure interpretation). No-signalling/decoherence/uncertainty framing verified; the no-cloning/no-deleting cross-refs to §5.13/§5.14 are correct (re-confirmed the section numbering).

### Ch 36 — How to Judge Claims — **Fix** (new finding)
The body is excellent — the five red flags, the dequantization-cascade case study, the hardware/algorithm/benchmark/vendor reading checklists, the physical-scaling reframing of "advantage by 2030," and the Sycamore/Schnorr/IBM-utility/boson-sampling case studies are all sharp and honest. **Two issues remain (not previously fixed):**
- **Fix (§36 "How to read," line 9):** the roadmap says "§36.3 walks the supremacy/advantage/utility saga … §36.4 covers the dequantisation pattern," but the **actual** §36.3 is the dequantisation/"quantum-inspired" section, §36.4 is "NISQ Marketing vs. Capability," and the supremacy case studies live in **§36.7**. The section numbers in the roadmap are crossed; update them.
- **Minor (§36.3):** "Hamiltonian simulation survives, because the speedup is over *any* classical algorithm for a problem that is **provably classically hard**" — the same "provably hard" overreach that was softened in §16.1. Local-Hamiltonian simulation is BQP-complete, so the hardness is conditional on `BQP≠BPP`; qualify to match §16.1's wording ("complexity-grounded / best-known-classical / under standard assumptions").

### Ch 37 — Endgame — OK (with applied fix)
Confirmed: HHL no longer grouped with Grover's polynomial speedup — stated as **exponential in the dimension under restrictive assumptions**, throttled by data-loading/conditioning/readout. Calibrated closing synthesis. **Ledger:** §37.2–§37.3 dated forecasts and the small-molecule-chemistry qubit estimate.

## Appendices

### App A — Notation Reference — OK
Consistent with the book-wide conventions (big-endian tensor order, conjugate-linear inner product, QFT negative sign, norm-bar glyphs). Maintenance: keep the Chapter-4 anchors and Qiskit `QFTGate` sign note aligned as headings/SDK move.

### App B — Common Gates — OK
Gate matrices match the MSB-first convention; endian warnings well placed. Minor wording note: §B.3 "All four are unitary" follows a display mixing `P(φ)` with `S, S†, T, T†` — the referent is clear but slightly loose. Versioning maintenance: the "verified against Qiskit 2.x" notes are appropriate but perishable (**ledger**).

### App C — Identities and Decompositions — OK (with applied fixes)
Confirmed: §C.2 the tensor Hermitian/unitary statement is now **one-way** (`A,B` Hermitian/unitary ⇒ `A⊗B` is, converse fails up to a scalar); §C.5 the Hadamard Euler factorization corrected to **`H = e^{iπ/2} R_y(π/2) R_z(π)`** (the old `R_z(π/2)R_y(π/2)R_z(π/2)` did not equal `H`). Pauli/CNOT/CZ/SWAP/KAK/MUB content verified.

### App D — Suggested Reading — OK
Sensible curated bibliography. Inherently perishable on the SDK/tooling and "current as of 2026" entries (**ledger**); the subjective "strongest"/"fastest" tooling language is an editorial choice. Minor: verify the Tang/dequantization entry's title/authorship pairing.

### App E — Glossary — OK (with applied fixes)
Confirmed: §E.7 density-matrix simulator gives **~half** the qubit budget of a statevector simulator (was "twice"); statevector entry aligned to `≈16 GB at 30 qubits` (was "30–40 on commodity hardware"). The amplitude-amplification `a` entry is internally correct (explicitly "success **amplitude**," so `O(1/a)` vs `O(1/a²)`). **Ledger:** 2026 gate-error and SDK specifics.

### App F — Hardware Snapshot 2026 — OK with ledger caveat
Correctly framed as the perishable snapshot with strong "treat every figure as dated" warnings. Two known items deferred to the dated fact-check pass and confirmed still open: the **snapshot date alternates** "early 2026"/"May 2026" (pick one), and the **Pasqal end-of-2025 roadmap** item is phrased as a future target although Pasqal passed 1,000+ atoms in 2024 (web-confirmed) — report it as achieved. Vendor/platform figures throughout are the highest-priority **ledger** targets before publication.

## Summary and Disposition

The book is in strong shape. Across all 47 files the mathematics, physics, complexity, and notation are careful and largely correct; the prose is dense and honest (notably about where speedups do and don't survive). Every correctness fix applied earlier in this 2026-05-30 session was re-confirmed in place and holds up on re-read.

### New actionable findings from this pass (not previously logged)

- **§2.6 — Fix.** "Two-qubit gate **fidelities** at the `10⁻²` to `10⁻³` level" / "approaching `10⁻³` to `10⁻⁴`" are **error rates**, not fidelities; contradicts the same section's line 96. (Same class as the §34.4 fix.)
- **§36 "How to read" — Fix.** The roadmap cross-labels sections: it says §36.3 = supremacy/advantage/utility saga and §36.4 = dequantisation, but actually §36.3 = dequantisation, §36.4 = NISQ marketing, and the case studies are in §36.7. Update the section numbers.
- **§36.3 — Minor.** "Hamiltonian simulation … provably classically hard" repeats the "provably hard" overreach softened in §16.1; qualify (BQP-complete, conditional on `BQP≠BPP`).
- **§1.5.1 — Minor.** `2^√n` pinned to GNFS, but GNFS is `L[1/3]` (faster than `2^√n ≈ L[1/2]`).
- **§7.9 — Minor.** The explicitly stated CHSH `B`-settings give `S=+2√2`, not the text's "sum to `−2√2`" (`|S|=2√2` saturates regardless; the text disclaims the sign, but sanity-check 5 asks for exactly these correlators).
- **§7.1–7.2 — cosmetic.** A displayed `|x₀…x_{n-1}⟩` label uses 0-indexing where §2.2 says formulas are 1-indexed (leftmost-MSB holds either way).
- **§8 sanity-check 3 — cosmetic.** `R_Y(π)|0⟩` equals `|1⟩` exactly, not "up to a global phase" (the phrasing is harmless; the intended `R_Y(π)=−iY≠X` point is right).
- **App B §B.3, App D Tang entry — cosmetic/maintenance.**

### Standing buckets (unchanged from the reviewer close-out)

- **Moving-target / dated-fact-check coverage.** Several perishable chapters (§2.6, §16/§17.8/§17.13, §19, §20, §22, §23, §25–§31, §33, §37) and Appendices D/F carry 2025–2026 vendor/standard/SDK/hardware numbers; some have local moving-target warnings, some rely on the ledger. This is the largest remaining *policy* item and is exactly what the `factcheck/` directory is built to track. Appendix F is the highest-priority target (incl. the snapshot-date inconsistency and the stale Pasqal end-2025 roadmap framing).
- **`INSTRUCTIONS.md`** still sets *global* git identity to the maintainer (doc hygiene; outside the book body, noted for completeness).

### Bottom line

No further hard correctness defects beyond the items above. The two new **Fix** items (§2.6 fidelity-vs-error-rate, §36 roadmap section numbers) are contained, independent edits; the rest are minor/cosmetic precision tweaks. After those, the remaining work is the perishable-claims fact-check pass and the warning-coverage policy decision — not core correctness.

---

*End of review — 2026-05-30 03:56 pass. Covered: front matter (3), Chapters 1–37, Appendices A–F (47 files total).*
