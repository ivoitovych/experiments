# Project Review - 2026-05-28 01:05

## Scope

Reviewed the Markdown project at `/home/i/exp/quantum_book`. The originally mentioned path `~/exp/quantoom_book` does not exist under `~/exp`; this review covers the available `quantum_book` project.

Requested order was followed: root-level Markdown files first, then Markdown files under `book/`, processed one by one in sorted path order. The review focused on project/documentation consistency, manuscript structure, status metadata, navigation, obvious placeholder/stale markers, and local build/lint health. It did not independently fact-check every technical or time-sensitive quantum-computing claim against external sources.

## Latest Two-Commit Review - 2026-05-29 00:40 CEST

Reviewed the full `git show --patch HEAD~2..HEAD` for:

- `087154c` — `Document the rendered-book toolchain + troubleshooting (reviewer ask)`.
- `5d61de9` — `Disposition: record fourth-reconciliation commit hash`.

### What changed

- `PROCESS.md` now has a dedicated `Building the rendered book (make book) — versions and troubleshooting` section. It answers the previously open ownership questions directly: supported `mdbook` range is `>=0.4,<0.5`, tested with `0.4.48`; required `mdbook-katex` is `0.9.4`; `book-build/` is safe to delete and is not the cause of the failure; `mdbook 0.5.x` support is explicitly out of scope for now; future `mdbook 0.5.x` support belongs in build configuration/preprocessor choice, not manuscript edits.
- `README.md` now points readers from the short mdBook setup section to `PROCESS.md` for the full version matrix, troubleshooting, and downgrade commands.
- `docs/review-2026-05-28-disposition.md` now records that the fourth reconciliation's build/tooling documentation ask was applied in `087154c`; `5d61de9` only replaces the placeholder commit id with the real hash.

### Verification

- `git status --short` was clean before this review pass.
- `python3 scripts/build_book.py --selftest` passed: `selftest OK (5 math + 7 link cases, SUMMARY lists all 47 chapters)`.
- `make lint` passed: `OK — 47 manuscript files checked`.
- `make book` still fails locally under `mdbook v0.5.3` + `mdbook-katex 0.9.4`, but now prints the intended compatibility warning and pinned-toolchain guidance. This is consistent with the documented support policy; it is no longer an unclear repository-instructions problem.

### Current assessment

- The maintainers have resolved the prior "who owns this / what should users do?" ambiguity. The project now clearly says `mdbook 0.4.x` is the supported line and `mdbook 0.5.x` is unsupported for now.
- The observed local build failure is not evidence of an Ubuntu mismatch. It is the expected result of running an unsupported `mdbook 0.5.x` toolchain with `mdbook-katex 0.9.4`.
- Minor documentation polish remains: the `README.md` short install snippet omits `--force`, while `PROCESS.md` correctly includes `--force` for downgrade/reinstall. This is not a blocker because README links to PROCESS for downgrade commands, but adding `--force` to the README snippet or explicitly saying "use PROCESS if replacing an existing mdbook" would reduce friction.

## Previous Post-Pull Reconciliation - 2026-05-29 00:26 CEST

After another pulled revision, I rechecked the remaining open items from the 2026-05-29 00:17 CEST reconciliation and reran local checks. This section supersedes the earlier reconciliation sections below for Chapters 8-15 and build/tooling status.

Commands run for this reconciliation:

- `git status --short`
  - Before reconciliation: clean.
- `mdbook --version`
  - `mdbook v0.5.3`.
- `mdbook-katex --version`
  - `mdbook-katex 0.9.4`.
- Targeted `rg` and `sed` checks against the still-open Chapter 8-15 items.
- `python3 scripts/build_book.py --selftest`
  - Passed: `selftest OK (5 math + 7 link cases, SUMMARY lists all 47 chapters)`.
- `make lint`
  - Passed: `OK — 47 manuscript files checked`.
- `make book`
  - Still fails locally during `mdbook-katex` with `invalid type: null, expected any valid TOML value at line 1 column 171`.
  - The build script still prints the expected compatibility warning: `mdbook 0.5.3` is outside the supported range `>=0.4, <0.5`; known-good is `mdbook 0.4.x` plus `mdbook-katex 0.9.4`. This remains a local toolchain-version blocker, not a manuscript/lint blocker.

### Newly fixed or materially improved since the prior reconciliation

- Chapter 12 §12.4 materially fixed the state-merging resource-ledger concern. It now distinguishes the LOCC setting, quantum communication at rate `S(A|B)` when positive, entanglement gained when negative, classical communication as a separate nonzero resource, and explicitly says `S(A|B)` is the quantum cost/yield rather than the whole communication cost.
- Chapter 15 §15.1 materially fixed the Grover success-bound note. It now states the exact post-iteration probability `sin^2((2k+1)theta)`, derives the rounded-`k` guarantee `>= cos^2(theta) = 1 - M/N`, and distinguishes the small-`theta` floor variant as slightly weaker.

### Still open after the newest pull

- Chapter 8 §8.12's native-gate-set bullets remain time-sensitive and should be checked against dated vendor documentation before publication.
- Chapter 9 §9.1 still gives the compressed Qiskit endian/top-wire statement. It is improved by the independent-choice note, but the Qiskit convention should still be checked against current Qiskit documentation/examples before closing.
- Chapter 14 §14.2's Qiskit `StatevectorSampler` examples still need version/API verification.
- Chapter 15 §15.1's Qiskit `StatevectorSampler` code still needs version/API verification.
- Chapter 15 §15.3 RSA resource estimates are scoped as dated anchors, but the actual figures remain time-sensitive and need dated external fact-checking before publication.
- Chapter 15 §15.8 VQE shot-count claims are scoped as estimates, but they remain time-sensitive and need dated external fact-checking before publication.
- Chapter 15 §15.10 QML status claims are scoped as a dated snapshot, but still need dated external fact-checking before publication.
- Rendered book build remains blocked in this local environment until `mdbook` is downgraded/pinned to the supported 0.4.x line or `mdbook-katex`/config compatibility with `mdbook 0.5.x` is resolved.

### Build/tooling ownership gap

- It is not clear from the repository instructions whether maintainers expect users to fix the rendered-book failure by doing a clean rebuild, reinstalling components, downgrading `mdbook`, or waiting for repository-side compatibility work. A clean rebuild alone is unlikely to solve the observed failure because the error happens after source assembly, when `mdbook v0.5.3` invokes `mdbook-katex 0.9.4`; the script itself identifies that version pair as unsupported.
- Maintainers should provide an explicit setup/troubleshooting section that answers: required `mdbook` version range, required `mdbook-katex` version, whether `book-build/` should be deleted during troubleshooting, exact reinstall/downgrade commands, and who owns future compatibility with `mdbook 0.5.x`.
- Until those instructions exist, readers/builders have to infer intent from the warning emitted by `scripts/build_book.py`. That warning is useful, but it should be backed by documented installation guidance in the repo so the responsibility is unambiguous.

### Build/tooling reproduction note

- Maintainers should try to reproduce this with a clean checkout on Ubuntu 24.04 or any Linux host by installing `mdbook v0.5.3` and `mdbook-katex 0.9.4`, then running `make book`. The local reproduction environment was Ubuntu 24.04.4 LTS (`noble`), `rustc 1.95.0`, `cargo 1.95.0`, `mdbook v0.5.3`, and `mdbook-katex 0.9.4`.
- The available evidence does not point to an Ubuntu-version mismatch. `python3 scripts/build_book.py --selftest` and `make lint` pass, source assembly succeeds, and the failure starts when `mdbook` calls the `katex` preprocessor. The failing message is `invalid type: null, expected any valid TOML value at line 1 column 171`, followed by `The "katex" preprocessor exited unsuccessfully`.
- Most likely cause: `mdbook 0.5.x` changed the render-context/config stream in a way that `mdbook-katex 0.9.4` does not accept. The repo already emits a warning saying supported `mdbook` is `>=0.4,<0.5` and known-good is `mdbook 0.4.x` plus `mdbook-katex 0.9.4`; maintainers should confirm this by comparing `make book` under `mdbook 0.4.x` versus `mdbook 0.5.3`.
- If maintainers intend to support only `mdbook 0.4.x`, the repository should state that as a hard prerequisite. If they intend to support `mdbook 0.5.x`, the fix likely belongs in the generated mdBook configuration, the selected KaTeX preprocessor version, or replacement of `mdbook-katex` with a preprocessor compatible with mdBook 0.5.

## Previous Post-Pull Reconciliation - 2026-05-29 00:17 CEST

After another pulled revision, I rechecked the still-open items from the 2026-05-28 23:57 CEST reconciliation and reran the local build/lint commands. This section supersedes the earlier reconciliation sections below for Chapters 8-15 and the build/tooling status.

Commands run for this reconciliation:

- `git status --short`
  - Before reconciliation: clean.
- Targeted `rg` and `sed` checks against the previously open Chapter 8-15 items.
- `mdbook --version`
  - `mdbook v0.5.3`.
- `mdbook-katex --version`
  - `mdbook-katex 0.9.4`.
- `python3 scripts/build_book.py --selftest`
  - Passed: `selftest OK (5 math + 7 link cases, SUMMARY lists all 47 chapters)`.
- `make lint`
  - Passed: `OK — 47 manuscript files checked`.
- `make book`
  - Still fails locally during `mdbook-katex` with `invalid type: null, expected any valid TOML value at line 1 column 171`.
  - Material improvement: the build script now prints an explicit compatibility warning that `mdbook 0.5.3` is outside the supported range `>=0.4, <0.5`, names the known-good toolchain as `mdbook 0.4.x` tested with `0.4.48` plus `mdbook-katex 0.9.4`, and gives pinned `cargo install` commands. The remaining local blocker is the installed `mdbook 0.5.3`; downgrade/pin to the 0.4.x line before expecting rendered HTML to complete.

### Newly fixed or materially improved since the prior reconciliation

- Chapter 8 §8.3 fixed the measurement/conjugation issue. It now separates `HXH = Z` as operator conjugation from the actual X-measurement procedure, which is a single `H` before computational-basis readout.
- Chapter 9 §9.8's time-sensitive hardware-support paragraph is materially improved: it now explicitly calls itself a fast-moving snapshot and tells readers to check current device documentation.
- Chapter 13 §13.7 fixed the "uncorrected classical" wording. It now says to compare fault-tolerant end-to-end quantum runtime, including error-correction overhead, with the best practical classical runtime for the same instance.
- Chapter 15 §15.3's RSA resource-estimate section is materially improved: the anchor estimates are now explicitly labelled as a 2025-2026 moving-target snapshot rather than fixed thresholds.
- Chapter 15 §15.8's VQE measurement-cost section is materially improved: it now labels the shot-count figures as 2026 current-hardware estimates that are problem/ansatz dependent and expected to change.
- Chapter 15 §15.10's QML status section is materially improved: it now labels the 2026 verdicts as a dated snapshot in a fast-moving research area.
- Build/tooling documentation is materially improved: the project now warns about `mdbook 0.5.x` incompatibility with `mdbook-katex 0.9.4` and provides known-good pinned install commands.

### Still open after the newest pull

- Chapter 8 §8.12's native-gate-set bullets remain time-sensitive and should be checked against dated vendor documentation before publication.
- Chapter 9 §9.1 still gives the compressed Qiskit endian/top-wire statement. It is improved by the added independent-choice note, but the Qiskit convention should still be checked against current Qiskit documentation/examples before closing.
- Chapter 12 §12.4 still describes state merging as if `S(A|B)` is simply the quantum communication Alice sends when nonnegative; this should distinguish quantum communication, entanglement cost/gain, classical communication, and the fully quantum Slepian-Wolf/state-merging resource convention more carefully.
- Chapter 14 §14.2's Qiskit `StatevectorSampler` examples still need version/API verification.
- Chapter 15 §15.1 still uses the rounded/floor Grover iteration wording together with a `>= 1 - M/N` success bound; the exact bound should be checked and stated carefully.
- Chapter 15 §15.1's Qiskit `StatevectorSampler` code still needs version/API verification.
- Chapter 15 §15.3 RSA resource estimates are better scoped as dated anchors, but the actual figures are still time-sensitive and need dated external fact-checking before publication.
- Chapter 15 §15.8 VQE shot-count claims are better scoped as estimates, but they remain time-sensitive and need dated external fact-checking before publication.
- Chapter 15 §15.10 QML status claims are better scoped as a dated snapshot, but still need dated external fact-checking before publication.
- Rendered book build remains blocked in this local environment until `mdbook` is downgraded/pinned to the supported 0.4.x line or `mdbook-katex`/config compatibility with `mdbook 0.5.x` is resolved.

## Previous Post-Pull Reconciliation - 2026-05-28 23:57 CEST

After the newest pulled revision, I rechecked the open items from the prior reconciliation against the current manuscript. This section supersedes the earlier reconciliation sections below for Chapters 8-15 and the project-level build/status notes.

Commands run for this reconciliation:

- `git status --short`
  - Before reconciliation: clean.
- Targeted `rg` and `sed` checks against the previously open items in Chapters 8-15.
- `python3 scripts/build_book.py --selftest`
  - Passed: `selftest OK (5 math + 7 link cases, SUMMARY lists all 47 chapters)`.
- `make lint`
  - Passed: `OK — 47 manuscript files checked`.
- `make book`
  - Now reaches `mdbook`, because `mdbook v0.5.3` and `mdbook-katex 0.9.4` are installed.
  - Fails during the `katex` preprocessor with `invalid type: null, expected any valid TOML value at line 1 column 171`; the generated `book-build/book.toml` itself is minimal and valid-looking. This is now the active rendered-book build blocker, replacing the earlier `mdbook not found` blocker.

Tooling/versioning note for follow-up:

- The rendered-book failure appears to be a component compatibility or version-pinning issue, not a missing-package issue. The local machine has `mdbook v0.5.3` and `mdbook-katex 0.9.4`; `mdbook` invokes the `katex` preprocessor, then the preprocessor exits unsuccessfully while parsing the render context/config JSON/TOML stream.
- The project should document known-good versions for `mdbook` and `mdbook-katex`, preferably in a setup section or a checked-in tool-version file. A likely investigation path is to test the project with the mature `mdbook` 0.4.x line, for example `mdbook 0.4.52`, together with `mdbook-katex 0.9.4`, and compare that against `mdbook 0.5.3`.
- Suggested responsible-party action: reproduce with `mdbook v0.5.3` + `mdbook-katex 0.9.4`; if confirmed, either pin compatible versions or update the generated `book.toml`/preprocessor configuration so `mdbook-katex` accepts the render context emitted by the installed `mdbook`.

### Newly fixed or materially improved since the prior reconciliation

- The global status/navigation ordering issue appears fixed: sampled chapter/front/back-matter files now put the status block immediately after the H1 and navigation after the status block, matching `STYLE.md`.
- Chapter 8 §8.7 now qualifies multi-controlled-gate costs by clean/dirty ancilla and ancilla-free assumptions.
- Chapter 8 §8.9 now explains the exact-universality scope of `{H, S, CNOT, Toffoli}` via the `{H, Toffoli}` real-universal result and the one-qubit real-encoding overhead.
- Chapter 8 §8.11 now explicitly scopes Ross-Selinger-style `c approx 1` synthesis to Clifford+T `z`-rotation synthesis and distinguishes it from generic Solovay-Kitaev.
- Chapter 9 §9.7 now states deferred measurement as equality of final joint outcome distributions, not state-by-state channel equality.
- Chapter 9 §9.8 now distinguishes non-demolition mid-circuit measurement/reuse from destructive readout platforms and scopes the later discussion to non-demolition readout.
- Chapter 9 §9.9 now describes Pauli-frame tracking instead of implying all Pauli corrections are physically applied every cycle.
- Chapter 9 §9.10 no longer uses the problematic "logarithmic-depth circuit" phrasing for Solovay-Kitaev; the current wording is materially clearer about compiled depth scaling.
- Chapter 10 §10.1 now adds the missing measurement/interference caveat to the "every branch at once" explanation.
- Chapter 10 §10.4 now derives `Delta X = Delta Y = 1` for `Z` eigenstates and explains why the Robertson bound is vacuous there.
- Chapter 10 §10.6 now states the restricted partial-trace cyclicity identity explicitly.
- Chapter 10 §10.12 now distinguishes phase damping from uniformly random `Z` rotations and gives the matching probabilistic-`Z` channel.
- Chapter 11 §11.7 now distinguishes qubit-wise commuting Pauli grouping from globally commuting strings that need entangling Clifford joint diagonalisation.
- Chapter 12 §12.7 now correctly says only the upper Fuchs-van de Graaf bound is generally tight on pure states.
- Chapter 12 §12.9 now makes the i.i.d. source assumption for Schumacher compression explicit.
- Chapter 12 §12.10 now says the coherent-information maximization ranges over input states on `n` channel uses.
- Chapter 15 §15.3's migration rhetoric is now better scoped to long-lived secrets and CRQC arrival; the old "when, not whether" wording is no longer standing alone.

### Still open after the newest pull

- Chapter 8 §8.3 still mixes observable conjugation with measurement procedure by saying an X measurement is Z measurement preceded and followed by `H`, then dropping the trailing `H`.
- Chapter 8 §8.12's native-gate-set bullets remain time-sensitive and should be checked against dated vendor documentation before publication.
- Chapter 9 §9.1 still gives the compressed Qiskit endian/top-wire statement.
- Chapter 9 §9.8's hardware-support claims remain time-sensitive even though the destructive/non-demolition distinction is now fixed.
- Chapter 12 §12.4 still describes state merging as if `S(A|B)` is simply the quantum communication Alice sends when nonnegative; this should distinguish quantum communication, entanglement cost/gain, classical communication, and the fully quantum Slepian-Wolf/state-merging resource convention more carefully.
- Chapter 13 §13.7 still contains the "uncorrected classical" complexity-language issue; use a clearer end-to-end classical baseline phrase.
- Chapter 14 §14.2's Qiskit `StatevectorSampler` examples still need version/API verification.
- Chapter 15 §15.1 still uses the rounded/floor Grover iteration wording together with a `>= 1 - M/N` success bound; the exact bound should be checked and stated carefully.
- Chapter 15 §15.1's Qiskit `StatevectorSampler` code still needs version/API verification.
- Chapter 15 §15.3 still has time-sensitive RSA resource-estimate claims that need dated external fact-checking.
- Chapter 15 §15.8's VQE shot-count and "2026" practicality claims remain time-sensitive and need external fact-checking.
- Chapter 15 §15.10's QML-in-2026 status claims remain time-sensitive and need external fact-checking.
- Rendered book build now has a concrete local blocker: `mdbook-katex 0.9.4` fails under `mdbook v0.5.3` with a TOML parse error before HTML rendering completes. This needs a tooling/config compatibility fix or pinned compatible versions.

## Previous Post-Pull Reconciliation - 2026-05-28

After the latest pulled revision, I rechecked the current manuscript against the existing reconciliation notes. This section supersedes the earlier post-pull status section below for Chapters 8-15.

Commands run for this reconciliation:

- `git status --short`
  - Before reconciliation: clean.
- Targeted `rg` and `sed` checks against Chapters 8-15 and the current review notes.
- `python3 scripts/build_book.py --selftest`
  - Passed: `selftest OK (5 math + 7 link cases, SUMMARY lists all 47 chapters)`.
- `make lint`
  - Passed: `OK — 47 manuscript files checked`.
- `make book`
  - Assembled `book-build/` successfully; final rendered HTML is still not checked because `mdbook` is not installed on PATH.

### Newly fixed or materially improved since the prior reconciliation

- Chapter 11 §11.5 now has a good classical-shadows caveat: post-hoc observable choice is allowed, but the high-probability sample-complexity guarantee depends on the observable class, and an unbounded/adversarial post-hoc query set can erase the advantage.
- Chapter 11 §11.6 now states the mixed-state support condition for unambiguous discrimination precisely.
- Chapter 11 §11.7 fixed the readout-mitigation scaling note: the text now distinguishes naive `2^n` calibration/inversion from tensor-product calibration and factorized inversion.
- Chapter 12 §12.8 now says no-deleting/no-hiding preserve quantum information on unknown states and explicitly says this is not a formal Noether-style conservation law.
- Chapter 14 §14.6 now qualifies controlled powers of `U`: efficient closed-form powers can give polynomial implementations, but a black-box `U^{2^k}` generally costs `2^k` oracle calls.
- Chapter 14 §14.9 now gives a more careful dihedral/lattice discussion, including Regev-style reductions and approximation-regime caveats.
- Chapter 13 §13.1 no longer uses the over-absolute "no black-box query" wording; it now distinguishes raw per-shot information from global features extracted through interference.
- Chapter 13 §13.3 no longer contains the editorial "seam" phrase.
- Chapter 15 §15.4 now cites concrete ECDLP resource-estimate papers, materially improving the earlier unsupported comparative-resource statement.
- Chapter 15 §15.5 now distinguishes original HHL's condition-number scaling from newer block-encoded/QSVT methods with essentially linear `kappa` dependence.
- Chapter 15 §15.5's dequantization discussion is now qualified by comparable data-access assumptions, which fixes the earlier over-broad wording.
- Chapter 15 §15.6 now frames glued trees as an oracle/query-complexity separation and narrows the Szegedy-walk claim to spectral-gap/hitting/search settings.
- Chapter 15 §15.9 now caveats the `p -> infinity` QAOA statement with adiabatic scheduling assumptions and avoids implying arbitrary finite-depth QAOA is guaranteed to converge.

### Still open after the latest pull

- The global status/navigation ordering issue appears to remain: many chapter files still put navigation before the status block, while `STYLE.md` says status should immediately follow the H1.
- Chapter 8 §8.3 still mixes observable conjugation with measurement procedure by saying an X measurement is Z measurement preceded and followed by `H`, then dropping the trailing `H`.
- Chapter 8 §8.7's multi-controlled-gate cost statement remains broad and should be qualified by clean/dirty ancilla and relative-phase-Toffoli assumptions.
- Chapter 8 §8.9 still lists `{H, S, CNOT, Toffoli}` as a common universal set without explaining the exact-universality theorem and its scope.
- Chapter 8 §8.11 still presents Ross-Selinger-style `c = 1` synthesis as if it applies generically to `{H, T}` rather than the usual Clifford+T synthesis context.
- Chapter 8 §8.12's native-gate-set bullets remain time-sensitive and should be checked against dated vendor documentation before publication.
- Chapter 9 §9.1 still gives the compressed Qiskit endian/top-wire statement.
- Chapter 9 §9.7 still states deferred measurement as `Meas o C(U) = C(U) o Meas` in a way that may read as literal channel equality rather than equality of relevant outcome statistics.
- Chapter 9 §9.8 still defines mid-circuit measurement as leaving the qubit available for reuse, which is too absolute for destructive-measurement platforms. Its hardware-support claims also remain time-sensitive.
- Chapter 9 §9.9 still describes error correction as applying corrections each cycle; add Pauli-frame tracking to avoid a misleading hardware picture.
- Chapter 9 §9.10 still frames the Solovay-Kitaev cost in terms of a logarithmic-depth circuit, which can blur depth, gate count, and recursion overhead.
- Chapter 10 §10.1 still says a gate evaluates "on every branch at once"; this needs the usual interference/linearity qualification.
- Chapter 10 §10.4 still says saturation of the Robertson bound for `X` and `Y` "forces" `Delta X = Delta Y = 1`; this should be derived or weakened because expectation-value constraints matter.
- Chapter 10 §10.6 still invokes cyclicity of the partial trace in a way that needs the usual subsystem restriction stated explicitly.
- Chapter 10 §10.12 still says phase damping is operationally indistinguishable from uniformly averaged random `Z` rotations; that is too strong without a precise distribution/model.
- Chapter 11 §11.7 still treats qubit-wise commuting as the criterion for simultaneous measurability after local basis rotations; clarify the distinction from more general entangling-Clifford joint measurement.
- Chapter 12 §12.4 still describes state merging as if `S(A|B)` is simply the quantum communication Alice sends when nonnegative; this should distinguish quantum communication, entanglement cost/gain, and classical communication.
- Chapter 12 §12.7 still says "Either side becomes an equality on pure states"; only the upper bound is generally tight for pure states.
- Chapter 12 §12.9 still gives a simplified Schumacher-compression formulation around `rho^{otimes n}` and should make the source-model assumptions explicit.
- Chapter 12 §12.10 still writes the regularized coherent-information formula without clearly saying the maximization ranges over arbitrary `n`-use input states.
- Chapter 13 §13.7 still contains the "uncorrected classical" complexity-language issue; use a clearer end-to-end classical baseline phrase.
- Chapter 14 §14.2's Qiskit `StatevectorSampler` examples still need version/API verification.
- Chapter 15 §15.1 still uses `k = floor(pi/(4 theta))` and `>= 1 - M/N`; the Grover iteration-count and success-bound statement needs correction.
- Chapter 15 §15.1's Qiskit `StatevectorSampler` code still needs version/API verification.
- Chapter 15 §15.3 still has time-sensitive RSA resource-estimate claims that need dated external fact-checking.
- Chapter 15 §15.3 still says the post-quantum migration question is "when, not whether"; this should be softened or explicitly scoped to sufficiently valuable long-lived secrets and future cryptographically relevant quantum computers.
- Chapter 15 §15.8's VQE shot-count and "today's best processors" claims remain time-sensitive and need external fact-checking.
- Chapter 15 §15.10's QML-in-2026 status claims remain time-sensitive and need external fact-checking.

## Post-Pull Reconciliation - 2026-05-28

After the new revision was pulled, I rechecked the existing review findings against the current manuscript. This section is the current status for the previously reviewed notes through Chapter 15; older per-file notes later in this document should be treated as historical unless repeated here.

Commands run for this reconciliation:

- `git status --short`
  - Before reconciliation: clean.
- `tail -n 160 review-2026-05-28-0105.md`
- `find book -type f -name '*.md' | sort`
- Targeted `rg` and `sed` checks against Chapters 8-15 and the current front/back matter.
- `python3 scripts/build_book.py --selftest`
  - Passed: `selftest OK (5 math + 7 link cases, SUMMARY lists all 47 chapters)`.
- `make lint`
  - Passed: `OK — 47 manuscript files checked`.
- `make book`
  - Assembled `book-build/` successfully; final rendered HTML still not checked because `mdbook` is not installed.

### Fixed or materially improved

- Chapter 8 §8.1 no longer says superposition is directly `processable in parallel`; it now adds the needed interference caveat.
- Chapter 8 §8.14's KAK/CNOT-cost summary was materially improved: it now uses the `c_z = 0` face, separates local/CNOT-like/generic cases, and distinguishes CNOT-native from iSWAP/fSim-native cost.
- Chapter 9 §9.4 fixed the compute-copy-uncompute composition order.
- Chapter 9 §9.9 fixed the magic-state/T-gate correction wording from a Pauli correction to a measurement-conditioned Clifford `S` correction.
- Chapter 9 §9.12 now qualifies the three-CNOT SWAP cost as CNOT-native and mentions native iSWAP/fSim alternatives.
- Chapter 10's `Parts 6 and 7` typo is fixed to `Parts 8 and 9`.
- Chapter 10 §10.6 now correctly distinguishes measurement/Kraus operators from POVM effects.
- Chapter 10 §10.8 now says syndrome extraction projects onto syndrome subspaces and paired correction returns to the code space.
- Chapter 10 §10.11 now uses a depolarising rate `gamma` in the Lindblad example instead of channel probability `p`.
- Chapter 10 §10.12 fixed the depolarising-channel parameterization and now gives matching Kraus operators.
- Chapter 11 §11.1 changed the non-demolition single-qubit example to weak/partial measurement.
- Chapter 11 §11.4 now says process tomography uses an informationally complete set of `4^n` input states.
- Chapter 11 §11.7 fixed the `Y` basis rotation to `H S^dagger` and replaced `prepared then discarded for I` with `no gate for I`.
- Chapter 12 §12.13's no-cloning/repetition wording is now limited to copying a prepared quantum input state, not ordinary classical reruns.
- Chapter 12 sanity checks 3 and 5 were corrected.
- Chapter 13 §13.7 now correctly states the standard belief that `P = BPP` is widely expected, while `BPP subsetneq BQP` and `BQP subsetneq PSPACE` are believed but unproved.
- Chapter 14 §14.4 now explicitly says Simon's exponential speedup is an oracle/query-model separation.
- Chapter 14 §14.5 and sanity check 3 now consistently use the negative-exponent QFT convention.
- Chapter 14 §14.7 now uses `I - 2P_good` for the marked-state phase flip.
- Chapter 14 §14.8 now defines amplitude estimation as estimating the success probability `a = sin^2(theta)`.
- Chapter 14 §14.9 removed the unclear separate factoring line and qualifies the old sweeping HSP claim with oracle-model exceptions.
- Chapter 15 §15.2 now uses `ML-KEM (FIPS 203)` and `ML-DSA (FIPS 204)`.
- Chapter 15 §15.3 replaced the inconsistent `3 x 10^6 logical qubits` resource line with anchor estimates and says the logical circuit uses a few thousand logical qubits.
- Chapter 15 §15.5 now describes HHL as a coherent spectral transformation rather than an explicit eigendecomposition.
- Chapter 15 §15.6 replaced the `§16.x` placeholder with `§16.5`.
- Root/process docs: `PROCESS.md` now uses `47 files total`; the current-facing 45-file process note is fixed. The remaining `45 files` wording found in `HISTORY.md` appears to be historical context, not current process guidance.
- Root/TOC docs: the stale `TOC.md` note saying the Index had not been produced was not found; treat that old finding as fixed.
- Root/style docs: the stale `STYLE.md` sentence saying only Chapters 8-9 figures are done was not found; treat that old finding as fixed.
- Build script: `python3 scripts/build_book.py --selftest` no longer emits the earlier `SyntaxWarning: invalid escape sequence '\{'`; treat that old finding as fixed.

### Still open / still current

- The global status/navigation ordering issue remains. Many files still put navigation before the status block, while `STYLE.md` says status should immediately follow the H1.
- Chapter 8 §8.3 still mixes observable conjugation with measurement procedure by saying an X measurement is Z measurement preceded and followed by `H`, then dropping the trailing `H`.
- Chapter 8 §8.7's multi-controlled-gate cost statement is still broad and should be qualified by clean/dirty ancilla and relative-phase-Toffoli assumptions.
- Chapter 8 §8.9 still lists `{H, S, CNOT, Toffoli}` as a common universal set without explaining the exact universality theorem.
- Chapter 8 §8.11 still presents Ross-Selinger-style `c = 1` synthesis as if it applies generically to `{H, T}` rather than the usual Clifford+T synthesis context.
- Chapter 8 §8.12's native-gate-set bullets are more detailed now, but they remain time-sensitive and need dated external fact-checking.
- Chapter 9 §9.1 still gives the compressed Qiskit endian/top-wire statement.
- Chapter 9 §9.7 still states deferred measurement as `Meas o C(U) = C(U) o Meas` in a way that may read as literal channel equality.
- Chapter 9 §9.8 still defines mid-circuit measurement as leaving the qubit available for reuse, which is too absolute for destructive-measurement platforms. Its hardware-support claims remain time-sensitive.
- Chapter 9 §9.9 still describes error correction as applying corrections each cycle; add Pauli-frame tracking to avoid a misleading hardware picture.
- Chapter 9 §9.10 still ties a `log(1/epsilon)` depth factor to Solovay-Kitaev for many practical algorithms without clearly limiting that to fault-tolerant discrete-gate compilation.
- Chapter 9 sanity check 2 still needs a superposed data-register condition if it is meant to force data/ancilla entanglement.
- Chapter 10 §10.1 still uses the `every branch at once` phrasing.
- Chapter 10 §10.4 still says sharp `Z` value `forces` `Delta X = Delta Y = 1`; this should be framed as an explicit Pauli-eigenstate calculation.
- Chapter 10 §10.6 still relies on cyclicity of the partial trace without explaining the restriction to operators on the traced subsystem.
- Chapter 10 §10.12 still says phase damping is operationally indistinguishable from random `Z` rotations averaged uniformly; uniformly random phase would fully dephase.
- Chapter 11 §11.5 still needs a caveat that the high-probability classical-shadow guarantee depends on the queried observable set.
- Chapter 11 §11.6 still overstates unambiguous discrimination for mixed states; the support condition should be stated precisely.
- Chapter 11 §11.7 still says qubit-wise commutation is `iff` for joint measurability, which is only true for the local basis-rotation workflow.
- Chapter 11 §11.7 still uses `O(2 n)` for tensor-product readout mitigation; clarify whether this means `O(n)` calibration settings or something else.
- Chapter 12 §12.4 still appears to conflate state-merging entanglement cost/gain with quantum communication cost.
- Chapter 12 §12.7 still says either side of the Fuchs-van de Graaf inequalities becomes equality on pure states; only the upper bound is generally tight for pure states under the unsquared fidelity convention.
- Chapter 12 §12.8 still phrases no-cloning/no-deleting as quantum information being `conserved`; keep the intuition but avoid sounding like a formal conservation law.
- Chapter 12 §12.9's Schumacher compression statement is still simplified around mixed `rho^{otimes n}` reconstruction rather than ensemble/purification and entanglement/average fidelity.
- Chapter 12 §12.10 still should clarify that the `max_rho` in the regularized quantum-capacity formula ranges over input states to `N^{otimes n}`.
- Chapter 13 §13.1 still says no black-box query yields more information than a single classical query; this is too absolute for promise-oracle algorithms.
- Chapter 13 §13.3 still uses the metaphorical phrase `the seam between query-model and uniform-circuit-model results`.
- Chapter 13 §13.7 still says compare fault-tolerant quantum running time to `uncorrected` classical running time; use a clearer end-to-end classical baseline phrase.
- Chapter 14 §14.2 and Chapter 15 §15.1 still include Qiskit `StatevectorSampler` snippets that should be verified against the pinned/target Qiskit version.
- Chapter 14 §14.6 still says a simple gate's powers can be implemented in `O(1)` by squaring; this should be limited to families with a closed-form efficient powered implementation.
- Chapter 14 §14.9's dihedral/lattice sentence is still broad and should specify which lattice reductions/approximation regimes are meant.
- Chapter 15 §15.1 still uses the simplified Grover iteration formula and success bound; check against the standard nearest-integer formula.
- Chapter 15 §15.3's RSA-2048 resource estimates are improved but remain time-sensitive and should be externally fact-checked.
- Chapter 15 §15.3 still says post-quantum migration is `when, not whether`; this is fine as risk framing but should be explicitly tied to the assumption of sufficiently large fault-tolerant quantum computers.
- Chapter 15 §15.4's ECDLP/RSA comparative resource claim still needs citation or softening.
- Chapter 15 §15.5 still uses original/simple HHL condition-number scaling and broad dequantization language; qualify against newer block-encoded linear-system algorithms and specific data-access assumptions.
- Chapter 15 §15.6 still frames glued-trees without explicitly saying oracle/query-model graph access, and the Szegedy/mixing-time claim remains broad.
- Chapter 15 §15.8's VQE shot-count examples and Chapter 15 §15.10's QML `2026 picture` remain current-state claims requiring dated fact-checking.
- Chapter 15 §15.9's `p -> infinity` QAOA convergence statement still needs a caveat that this assumes an adiabatic-style schedule and does not imply efficient learnability of optimal finite-depth parameters.
- Full rendered HTML remains locally unverified because `mdbook`/`mdbook-katex` are not installed on PATH. `make book` only assembles the build tree in this environment.

## Commands Run

- `git status --short`
  - Before review: clean except for this untracked review file after it was created.
  - After review: only `review-2026-05-28-0105.md` is untracked.
- `make lint`
  - Passed: `OK - 47 manuscript files checked`.
- `make progress`
  - Passed/regenerated: `wrote PROGRESS.md (47 entries)`.
- `python3 scripts/build_book.py --selftest`
  - Passed: `selftest OK (5 math + 7 link cases, SUMMARY lists all 47 chapters)`.
  - Warning: Python emits `SyntaxWarning: invalid escape sequence '\{'` from `scripts/build_book.py:2`.
- `make book`
  - Assembled `book-build/` successfully with 47 chapters.
  - Did not render final HTML because `mdbook` is not installed on PATH.
  - `book-build/` is ignored by `.gitignore`.

## Key Findings

### Finding 1 - Current process/history docs still contain stale 45-file language

`PROGRESS.md` and the current `book/` tree report 47 manuscript files, but current-facing prose still contains older 45-file wording.

Locations:

- `PROCESS.md:21-22` says: `Forty-five files total: thirty-seven chapters plus three front-matter files and five appendices.`
- `PROCESS.md:257` says: `One Markdown file per chapter (45 files total).`
- `HISTORY.md:42` says: `per-chapter (45 files: 37 chapters + 3 front-matter + 5 appendices).`
- `HISTORY.md:59` says the tree and `45 stub files` were generated.

Impact: confusing for a reader trying to understand the current manuscript inventory. `HISTORY.md` can retain historical wording if clearly framed as the original state, but `PROCESS.md` reads as current process documentation and should be updated.

Suggested fix: update current-facing references to 47 files: 37 chapters, 3 front-matter files, appendices A-F, and index/back matter. For `HISTORY.md`, either leave early history as-is but add a short note that Appendix F and Index brought the manuscript to 47 files, or revise the wording to avoid looking current.

### Finding 2 - `TOC.md` reconciliation note says the Index has not been produced, but it exists

`TOC.md:67` says: `The "Index" entry listed under Back Matter has not yet been produced.`

Current state:

- `book/99-back-matter/index.md` exists.
- It is marked `draft`.
- `PROGRESS.md` includes it in Phase 6.
- `HISTORY.md` says the Index became real and has 51 entries.

Impact: the reconciliation note is now stale and contradicts the delivered manuscript.

Suggested fix: update the `TOC.md` reconciliation note to say the Index has been produced, and mention Appendix F if the note is intended to catalogue all delivered-layout changes from the original outline.

### Finding 3 - `scripts/build_book.py` emits a Python syntax warning

Both `python3 scripts/build_book.py --selftest` and `make book` emit:

```text
scripts/build_book.py:2: SyntaxWarning: invalid escape sequence '\{'
```

Cause: the module docstring contains literal LaTeX/backslash examples such as `\{` and `\}` in a normal Python string literal.

Impact: harmless for runtime today, but noisy and easy to confuse with a real build problem.

Suggested fix: make the module docstring raw by changing the opening triple quote from `"""` to `r"""`.

### Finding 4 - `STYLE.md` understates the current figure coverage

`STYLE.md` says: `Circuit figures for Chapters 8-9 are done.`

Current project history says the figure pipeline now covers 15 SVGs across Chapters 7-10, 14-15, and 19. The existing style statement is not wrong about Chapters 8-9, but it is stale and undersells the actual figure coverage.

Suggested fix: replace the sentence with a current, less brittle statement such as: `Several circuit figures are generated and embedded across the manuscript; see figures-src/generate_figures.py for the authoritative list.`

### Finding 5 - The full mdBook HTML build is not locally verifiable in this environment

`make book` assembles the mdBook source tree successfully, but stops at:

```text
mdbook not found on PATH; assembled sources only. Install with `cargo install mdbook mdbook-katex`, then re-run `make book`.
```

Impact: local review could verify the assembler and self-tests, but not the final rendered HTML, final link graph, or final KaTeX output.

Suggested fix: install the pinned mdBook toolchain before final release/review, then rerun `make book` and verify zero broken links / zero KaTeX errors as described in project history.

### Finding 6 - Time-sensitive 2025/2026 claims need a source-verification pass before `reviewed`

The manuscript intentionally contains many current-state claims: hardware metrics, roadmap timing, post-quantum cryptography status, vendor demonstrations, satellite QKD missions, resource estimates, and advantage/utility case studies.

Examples surfaced during the book pass include:

- `book/part-06-algorithms/15-landmark-quantum-algorithms.md` resource estimates and 2025-2026 factoring/VQE/QAOA claims.
- `book/part-06-algorithms/16-modern-algorithmic-frontier.md` 2026 algorithm-frontier claims.
- `book/part-09-hardware-and-software/20-quantum-hardware-platforms.md` current hardware/platform claims, including topological-qubit/Majorana framing.
- `book/part-12-adjacent-models/33-quantum-communication-and-networking.md` satellite-QKD and networking-tooling claims.
- `book/part-13-perspective-and-direction/36-how-to-judge-claims.md` 2019-2026 case studies and roadmap interpretation.
- `book/part-13-perspective-and-direction/37-endgame.md` state-of-field and timeline claims.
- `book/99-back-matter/appendix-f-hardware-snapshot-2026.md` is explicitly perishable.

Impact: appropriate for a 2026 draft, but these are the highest-risk claims to verify before promoting affected chapters from `draft` to `reviewed` or `final`.

Suggested fix: add a dedicated dated fact-check pass for current claims, ideally producing a source ledger for Appendix F and the hardware/roadmap/security/application chapters.

## Root Markdown Files Processed

| File | Review notes |
|---|---|
| `BookDescription.md` | Has a clear reconciliation note warning that the delivered manuscript is source of truth. Some later sections intentionally describe the original plan; acceptable because of the note. It depends on `TOC.md` for full reconciliation, so the stale Index note in `TOC.md` weakens this handoff. |
| `HISTORY.md` | Strong project narrative and useful decision history. Contains stale/historical 45-file language at lines 42 and 59; later sections correctly describe Appendix F, Index, and 47/47 progress. Consider qualifying early 45-file references as original bootstrap state. |
| `INSTRUCTIONS.md` | Short, clear commit-identity rules. No issue found. |
| `PROCESS.md` | Useful current process documentation. Contains current-facing stale file-count/back-matter language: 45 files / five appendices. This should be updated because `PROGRESS.md` and the manuscript now have 47 entries. |
| `PROGRESS.md` | Regenerates cleanly and matches the current manuscript count: 47 files, all at `draft`. No issue found. |
| `README.md` | Current, reader-facing, and consistent with the delivered manuscript. Links and status framing are good. No issue found. |
| `STYLE.md` | Good conventions document. Figure coverage statement is stale/under-complete now that generated figures span more than Chapters 8-9. |
| `TOC.md` | Useful as an original planned outline with reconciliation note. Stale issue: reconciliation note says the Index has not yet been produced, but `book/99-back-matter/index.md` exists and is `draft`. |

## `book/` Markdown Files Processed

Processed one by one in sorted path order. All files have status blocks, all have two `[Table of Contents]` navigation links, and `make lint` passes across the full set.

| File | Review notes |
|---|---|
| `book/00-front-matter/00-preface.md` | Structurally OK. Has an extra unnumbered note section relative to status count; this is expected front matter, not a defect. |
| `book/00-front-matter/01-background-and-self-check.md` | Structurally OK. No placeholder markers found. |
| `book/00-front-matter/02-notation-and-conventions.md` | Structurally OK. Mentions lifecycle states intentionally; no issue. |
| `book/99-back-matter/appendix-a-notation-reference.md` | Structurally OK. |
| `book/99-back-matter/appendix-b-common-gates.md` | Structurally OK. |
| `book/99-back-matter/appendix-c-identities-and-decompositions.md` | Structurally OK. |
| `book/99-back-matter/appendix-d-suggested-reading.md` | Structurally OK. Suggested reading likely deserves periodic URL/source freshness review before final publication. |
| `book/99-back-matter/appendix-e-glossary.md` | Structurally OK. |
| `book/99-back-matter/appendix-f-hardware-snapshot-2026.md` | Structurally OK, but deliberately time-sensitive. Should be fact-checked close to release. |
| `book/99-back-matter/index.md` | Exists and is `draft`; this contradicts `TOC.md`'s stale reconciliation note saying the Index has not yet been produced. |
| `book/part-01-orientation/01-why-quantum-computing-exists.md` | Structurally OK. Contains a bridge section beyond the planned six sections, matching the reconciliation note pattern. |
| `book/part-01-orientation/02-classical-to-quantum-contrast.md` | Structurally OK. Contains current hardware/error-rate statements that should be part of the dated fact-check pass. |
| `book/part-01-orientation/03-physical-intuition.md` | Structurally OK. Contains a bridge section; no issue. |
| `book/part-02-formalism/04-mathematical-background.md` | Structurally OK. This is the most heavily reviewed chapter according to project history. |
| `book/part-02-formalism/05-postulates.md` | Structurally OK. Contains a bridge section beyond planned count. |
| `book/part-03-qubits/06-the-qubit.md` | Structurally OK. Contains a bridge section beyond planned count. |
| `book/part-03-qubits/07-multiple-qubits-and-entanglement.md` | Structurally OK. Contains a bridge section and generated teleportation figure. |
| `book/part-04-gates-and-circuits/08-quantum-gates.md` | Structurally OK. Figures are present in adjacent `figures/`. |
| `book/part-04-gates-and-circuits/09-quantum-circuits.md` | Structurally OK. Contains a bridge section. |
| `book/part-04-gates-and-circuits/10-core-quantum-phenomena.md` | Structurally OK. Contains a bridge section and content moved here from the original Chapter 12 plan, consistent with reconciliation notes. |
| `book/part-05-measurement-and-information/11-measurement-theory.md` | Structurally OK. Contains a bridge section. |
| `book/part-05-measurement-and-information/12-quantum-information-theory.md` | Structurally OK. Reorganized from original plan; current content matches project reconciliation notes. |
| `book/part-06-algorithms/13-quantum-algorithms-mindset.md` | Structurally OK. |
| `book/part-06-algorithms/14-foundational-algorithms.md` | Structurally OK. Contains a bridge section and generated algorithm figures. |
| `book/part-06-algorithms/15-landmark-quantum-algorithms.md` | Structurally OK. Contains 2025-2026 resource-estimate/application claims; include in fact-check pass. |
| `book/part-06-algorithms/16-modern-algorithmic-frontier.md` | Structurally OK. Contains 2026 frontier claims; include in fact-check pass. |
| `book/part-07-complexity/17-complexity-theory.md` | Structurally OK. No unresolved placeholder markers found. |
| `book/part-08-noise-and-qec/18-noise-decoherence-and-errors.md` | Structurally OK. Reorganized from original plan; current content matches reconciliation notes. |
| `book/part-08-noise-and-qec/19-quantum-error-correction-and-fault-tolerance.md` | Structurally OK. Has QEC figure coverage started. |
| `book/part-09-hardware-and-software/20-quantum-hardware-platforms.md` | Structurally OK. High fact-check priority because it contains current platform/vendor/hardware-status claims. |
| `book/part-09-hardware-and-software/21-quantum-control-and-electronics.md` | Structurally OK. Contains a bridge section. |
| `book/part-09-hardware-and-software/22-hardware-engineering-metrics.md` | Structurally OK. High fact-check priority for benchmark/metric claims. |
| `book/part-09-hardware-and-software/23-quantum-programming-compilation-and-tooling.md` | Structurally OK. Tooling/API landscape should be periodically checked before final publication. |
| `book/part-09-hardware-and-software/24-classical-simulation-of-quantum-systems.md` | Structurally OK. |
| `book/part-10-practice-and-era/25-nisq-and-early-fault-tolerant-era.md` | Structurally OK. High fact-check priority because it frames current-era capability. |
| `book/part-10-practice-and-era/26-practical-access-and-hands-on-work.md` | Structurally OK. Contains a bridge section. Cloud/SDK access details should be checked before final. |
| `book/part-11-applications/27-cryptography-and-security.md` | Structurally OK. High fact-check priority for NIST PQC/security-timeline statements. |
| `book/part-11-applications/28-scientific-computing-and-physical-simulation.md` | Structurally OK. Contains a bridge section. |
| `book/part-11-applications/29-optimization-finance-and-industrial.md` | Structurally OK. Current advantage/industrial-value claims should be checked before final. |
| `book/part-11-applications/30-quantum-machine-learning.md` | Structurally OK. Current QML advantage/dequantization claims should be checked before final. |
| `book/part-11-applications/31-quantum-sensing-metrology-and-tomography.md` | Structurally OK. |
| `book/part-12-adjacent-models/32-adjacent-computational-models.md` | Structurally OK. |
| `book/part-12-adjacent-models/33-quantum-communication-and-networking.md` | Structurally OK. Satellite-QKD/networking-tool claims are time-sensitive; include in fact-check pass. |
| `book/part-13-perspective-and-direction/34-bridging-to-familiar-engineering-ideas.md` | Structurally OK. |
| `book/part-13-perspective-and-direction/35-interpretational-and-conceptual-pitfalls.md` | Structurally OK. No unresolved placeholder markers found. |
| `book/part-13-perspective-and-direction/36-how-to-judge-claims.md` | Structurally OK. Contains current case-study/roadmap claims; include in fact-check pass. |
| `book/part-13-perspective-and-direction/37-endgame.md` | Structurally OK. Contains state-of-field and timeline claims; include in fact-check pass. |

## Overall Assessment

The project is in good structural condition for a full draft. The manuscript inventory is complete, lint passes, progress metadata regenerates, and the mdBook assembler self-tests pass. The main issues are not broken structure; they are stale top-level documentation created by the manuscript growing from 45 to 47 files, a stale `TOC.md` note about the Index, a noisy Python docstring warning, and the need for a dedicated source-backed fact-check pass on time-sensitive 2025/2026 claims before any chapter is promoted beyond `draft`.

---

## Comprehensive Manuscript Review Pass

This section is an incremental, file-by-file reading pass over the manuscript under `book/`. It is separate from the structural pass above. Notes here focus on reader experience, internal consistency, editorial risk, missing support, and claims that need follow-up before promotion from `draft` to `reviewed`.


### Comprehensive Notes - Front Matter

#### `book/00-front-matter/00-preface.md`

Read end to end. The preface is strong and does the right positioning work: anti-hype, explicit audience, clear prerequisites, and a useful warning that 2026 hardware numbers age quickly. It also establishes conventions and reading paths before the technical chapters begin.

Review findings:

- The file order contradicts `STYLE.md`: navigation appears before the status block, while `STYLE.md` says the status block should appear immediately after the top-level heading and before navigation. This is not unique to the preface, but the preface is the first visible instance.
- The line saying `Every chapter ends with a small set of sanity-check exercises` is mostly true for chapters, but not for front matter and not for the self-check file's answer style. Consider tightening to `Most technical chapters...` or `Each main chapter...` if front matter is considered part of the readable manuscript.
- The preface says answers are not printed inline, but `01-background-and-self-check.md` intentionally includes inline answers after sample problems. The distinction is understandable, but a reader may read it as a contradiction. Suggested fix: explicitly exempt the self-check chapter.
- The Qiskit endian warning in the preface is useful but high-risk. The sentence `a Qiskit 0b01 printout corresponds to this book's |10>` should be rechecked against the exact Qiskit context intended: statevector index, counts key order, classical-bit order, and circuit wire labels are different conventions. The deeper discussion in Chapter 4 may be correct, but this compressed version is easy to misread.
- The claim that the glossary collects every term introduced in boldface is operationally brittle. If not mechanically enforced, consider weakening to `many` or `major` terms.

#### `book/00-front-matter/01-background-and-self-check.md`

Read end to end. The chapter is useful and concrete: it calibrates Python, math, probability, complexity, and optional background without pretending to teach those subjects from scratch. The inline answers are appropriate here because calibration is the point.

Review findings:

- The opening says `the closing paragraph of each section names a standard reference`, but §1.2 does not have a sample problem or a clear closing reference in the same style as other sections. Either add a reference for mathematical maturity or soften the sentence.
- The self-check says `§1.6 collects them in one place`; this is true for major references, but §1.7 follows after it and gives DSP-specific framing. No defect, but the phrase may make §1.6 sound like the chapter's final section.
- The sample-problem answer style is very helpful, but it conflicts with the preface's later/earlier blanket statement that answers are not printed inline. See preface note.
- No unresolved placeholders or obvious structural problems found.

#### `book/00-front-matter/02-notation-and-conventions.md`

Read end to end. This is one of the most valuable front-matter files: it centralizes renderer constraints, notation choices, tensor-order conventions, probability notation, and source rules. The cross-links to Chapter 4 and appendices make it usable as both orientation and reference.

Review findings:

- Same global ordering issue: actual files use heading -> navigation -> status block, while `STYLE.md` prescribes heading -> status block -> navigation.
- The Qiskit endian discussion is important but should be checked carefully against all four Qiskit conventions. It correctly warns that statevector indexing, printed bit strings, circuit diagram order, and qubit labels differ, but the short examples elsewhere may be too compressed.
- The section says lint catches `kets in tables`; the current `tools/lint.py` checks several renderer hazards but does not obviously enforce every conventional item named here. Verify this claim against the actual lint implementation before relying on it.
- The source convention `the exact spacing and bullet character (·) are load-bearing` is stricter than the parser appears to require: `scripts/generate_progress.py` uses a regex over labels and numbers, not the exact middle-dot spacing. Either make the parser stricter or soften the prose.
- No unresolved placeholders found.


### Comprehensive Notes - Back Matter

#### `book/99-back-matter/appendix-a-notation-reference.md`

Read end to end. This is a useful compact reference and mostly aligns with the front-matter notation chapter. The decision to avoid tables because of ket/norm bars is well justified.

Review findings:

- Same global ordering issue: navigation appears before the status block, contrary to `STYLE.md`.
- Qiskit endian guidance is repeated here and should be validated as a single source-backed convention across Preface, §2.4, §4.8, Appendix A, and Appendix B. Repetition increases the risk that one compressed statement drifts.
- The appendix says circuit drawing conventions are introduced in Chapter 6, but the richer circuit-model treatment is Chapter 9 and gate notation is Chapter 8. The current wording is probably acceptable if Chapter 6 first introduces simple circuits, but verify the cross-reference intent.
- No obvious unresolved placeholders found.

#### `book/99-back-matter/appendix-b-common-gates.md`

Read end to end. The gate catalogue is clear, practical, and valuable as a lookup reference. The CNOT/CZ/SWAP distinction under MSB-first ordering is explained carefully.

Review findings:

- Same global status/nav ordering issue.
- The Qiskit-specific claims in §B.5 and §B.8 should be verified against the exact Qiskit version targeted by the runnable examples. These claims are plausible, but endian conventions are a high-risk source of subtle errors.
- The sentence `The basis states accumulate no relative observable phase from P(phi) on its own` in §B.3 is potentially too compressed: applying `P(phi)` to a superposition absolutely changes relative phase and is observable after interference. The next clause says the phase becomes observable after a basis change, but the wording may mislead. Consider saying `A computational-basis eigenstate accumulates only a global phase...` or `On a measured computational-basis input alone...`.
- The controlled-global-phase discussion in §B.9 is useful but the notation `CP(alpha) \otimes_{control}` is nonstandard/unclear. Consider replacing it with an explicit diagonal matrix or prose.

#### `book/99-back-matter/appendix-c-identities-and-decompositions.md`

Read end to end. This appendix is dense but useful: it collects exactly the identities a reader will want during algorithm, stabilizer, and compilation chapters.

Review findings:

- Same global status/nav ordering issue.
- The Pauli product table and commutator identities are consistent on review.
- The Toffoli decomposition paragraph is a little hand-wavy for an appendix entry: `standard T / T-dagger ladder` is not enough to reconstruct the circuit, while the rest of the appendix usually gives explicit formulas. Consider either linking to a concrete decomposition figure/section or reducing the claim to a gate-count summary.
- The statement `In prime-power dimensions d there are exactly d + 1 mutually unbiased bases` should be stated as `there exist d + 1` unless the intended meaning is `the maximum possible number is d + 1 and it is achieved in prime-power dimensions`. As written, `exactly` can be read as excluding smaller sets of MUBs, which exist trivially.

#### `book/99-back-matter/appendix-d-suggested-reading.md`

Read end to end. The bibliography is useful and well grouped. It gives a credible reader path beyond the book.

Review findings:

- Same global status/nav ordering issue.
- This appendix contains several live/tooling references (`quantum.cloud.ibm.com`, Qiskit object model, Q# tooling, tket docs, Braket docs). These should be URL/version checked before release because SDK documentation and branding change quickly.
- Some paper/topic descriptions use `current` or `still-current` language. That is acceptable in a 2026 draft, but should be included in the dated fact-check pass.
- The Qiskit note says older `QFT` class is superseded by `QFTGate` `as of writing`; this should be checked against the installed Qiskit version used by examples before finalizing.

#### `book/99-back-matter/appendix-e-glossary.md`

Read end to end. The glossary is readable and gives concise definitions without becoming a second textbook. Splitting `state` into multiple senses is a good choice.

Review findings:

- Same global status/nav ordering issue.
- Some glossary cross-references are broad chapter references rather than precise sections. That is acceptable for now, but if the glossary is expected to be a lookup tool, terms like `Ancilla`, `Circuit`, `Measurement`, and `Quantum channel` would benefit from exact section references.
- Hardware and tooling entries include current-state claims: `per-gate error rates of 10^-3 to 10^-4 are the current state of the art`, SDK ownership/maintenance statements, and simulator ceilings. These need the same dated fact-check pass as Appendix F.
- The fidelity entry says the book uses a squared-overlap convention for pure states and a generalized Uhlmann fidelity for density matrices. Because squared vs unsquared fidelity conventions are a known source of confusion, verify Chapter 12 states the convention as explicitly as the glossary promises.

#### `book/99-back-matter/appendix-f-hardware-snapshot-2026.md`

Read end to end. The appendix is appropriately framed as perishable and repeatedly warns readers not to treat hardware numbers as timeless specifications. That framing is good.

Review findings:

- Same global status/nav ordering issue.
- This appendix is the highest priority for external source verification before release. It includes specific claims about IBM Heron/Condor, Google Willow, Quantinuum H-series, IonQ, QuEra, Atom Computing, Pasqal, Xanadu, PsiQuantum, Intel, Microsoft Majorana 1, qLDPC roadmaps, and representative fidelity/gate-time numbers.
- The table in §F.6 uses Markdown table syntax with `~10-100 ns`, `~1-100 µs`, etc. It is simple enough to render, but the project generally avoids fragile tables for math-heavy content; this one is probably fine because it contains no kets/norm bars.
- §F.8 says `replace this appendix wholesale rather than patching individual numbers`; this is good process guidance and should be mirrored in the release checklist if one is added.

#### `book/99-back-matter/index.md`

Read end to end. The generated index exists, is readable, and is correctly framed as generated from `scripts/generate_index.py`.

Review findings:

- Same global status/nav ordering issue.
- This file directly contradicts the stale `TOC.md` reconciliation note saying the Index has not yet been produced.
- Some index targets are defensible but debatable as `where the term is developed`: e.g. `Born rule` points to §10.3 rather than the formal measurement-postulate treatment in Chapter 5 or measurement theory in Chapter 11; `Fidelity` points to Chapter 22 rather than the information-theory treatment in Chapter 12. Consider whether the index should prefer first formal definition, main conceptual development, or most practical use, and encode that policy in `scripts/generate_index.py` comments.


### Comprehensive Notes - Chapters 1-3

#### `book/part-01-orientation/01-why-quantum-computing-exists.md`

Read end to end. The chapter is effective as motivation: it separates hype from real stakes, introduces interference rather than parallelism, and frames NISQ vs fault tolerance clearly for a developer audience.

Review findings:

- Same global status/nav ordering issue.
- The sentence saying Shor's paper `broke the public-key cryptography that underwrites modern internet security` is rhetorically strong but temporally imprecise. Shor broke the mathematical assumptions in the fault-tolerant quantum model; deployed cryptography is not practically broken today. Consider `showed how to break... on a sufficiently large fault-tolerant quantum computer`.
- §1.6 includes several current hardware claims that need fact-checking before review/final status. In particular, `Quantinuum and IonQ ... machines with hundreds of qubits` appears too broad or at least ambiguous depending on whether it means physical ions, effective/algorithmic qubits, roadmap systems, or available devices.
- The chapter's forecast language around simulation advantage within `roughly five to ten years` should be either sourced or explicitly marked as a scenario rather than a prediction.
- The sanity checks are well targeted and reinforce the chapter's claims.

#### `book/part-01-orientation/02-classical-to-quantum-contrast.md`

Read end to end. The chapter does a good job contrasting bits/qubits, probability/amplitudes, reversibility, and measurement. The Deutsch example is a useful bridge into algorithms.

Review findings:

- Same global status/nav ordering issue.
- §2.5 says probabilistic computation is `no more powerful than the deterministic model up to polynomial overhead` and then correctly says `BPP is conjectured equal to P`. The first phrasing is too strong as a known theorem. Suggested fix: say randomized computation is *not believed* to add super-polynomial power, and that `BPP = P` is conjectured but open.
- §2.6 contains current hardware/error-rate claims that should be aligned with Appendix F. For example, superconducting two-qubit fidelities are described as `10^-2 to 10^-3` error level here, while Appendix F describes representative 2026 values closer to `~99.5-99.9%`. The ranges can be reconciled, but the wording should avoid sounding stale or inconsistent.
- §2.7 references `Superdense coding (§7.12)` and `Teleportation (§7.12)`. Verify those section references in Chapter 7; if Chapter 7 section numbers changed, this is a high-value cross-reference to keep exact.
- The final sanity-check item asks for `depth of a useful circuit before the state randomises` from 99% two-qubit fidelity. Good exercise, but `contrast with the depth needed for a fault-tolerant logical operation` may require knowledge not yet introduced. It is acceptable as a forward-looking prompt, but could frustrate first-pass readers.

#### `book/part-01-orientation/03-physical-intuition.md`

Read end to end. The chapter is strong pedagogically: Stern-Gerlach, polarization, Mach-Zehnder, double slit, sequential measurement, Bell tests, and decoherence form a coherent path into the postulates.

Review findings:

- Same global status/nav ordering issue.
- Opening `How to read` says the hardware chapters are in `Part VI`; they are in Part IX. This is a concrete typo.
- §3.3 says molecule-interference experiments include biological molecules of several thousand atoms. This is a memorable claim and should be source-checked or softened if not cited nearby.
- §3.8's Bell-test history is broadly well framed, but phrases like `every loophole that could conceivably let a local hidden-variable theory survive` should be treated carefully. Simultaneously loophole-free Bell tests close the standard experimental loopholes, but philosophical loopholes like superdeterminism are not experimentally closed in the same sense. Consider `every standard experimental loophole`.
- §3.9 contains current coherence-time/platform comparisons; include them in the dated hardware fact-check pass.
- The sanity checks are excellent and concrete.


#### `book/part-02-formalism/04-mathematical-background.md`

Read end to end. This is the most mature chapter so far: careful conventions, strong sanity checks, good handling of global/relative phase, tensor ordering, projectors, SVD, QFT sign, and the active/passive basis-change distinction. The `Common Traps` and `Conventions at a Glance` sections are especially valuable.

Review findings:

- Unlike most earlier files, this chapter follows the `STYLE.md` order: status block before navigation. This reinforces that the rest of the manuscript's nav/status order drift is real.
- Qiskit convention statements in §4.8, §4.13, and §4.16 should be verified against the exact Qiskit version used in examples. The chapter is appropriately cautious, but it contains specific claims about Qiskit 2.x `QFTGate`, final swaps, statevector indexing, and qubit-label mapping.
- §4.6 says more general measurements are covered in `Chapters 11-12`. POVMs/generalized measurement are mainly Chapter 11; Chapter 12 is information theory. The reference is not wrong if Chapter 12 uses channels/information measures, but it may be more precise as `Chapter 11, with channels/information-theoretic consequences in Chapter 12`.
- §4.14's Holevo and amplitude-estimation previews are useful, but these are advanced compared with the rest of the refresher. The chapter's opening tells readers they may skim this material, which is enough; no change required.
- No obvious mathematical errors found in the reviewed formulas. The tensor-order examples and Pauli/operator identities are internally consistent.


#### `book/part-02-formalism/05-postulates.md`

Read end to end. The chapter is clear and useful: it turns Chapter 4's linear algebra into operational postulates, then introduces density matrices, reduced states, partial trace, and no-cloning/no-deleting at the right point.

Review findings:

- This chapter follows the `STYLE.md` status-before-navigation order.
- §5.11 has a technical notation error in the operational characterization of the reduced state. It writes:
  `tr_A((M_A \otimes I_B) rho_AB) = tr(M_A rho_A)`.
  The left side should be the full trace over the joint system, e.g. `tr_{AB}((M_A \otimes I_B) rho_AB)`, or simply `tr((M_A \otimes I_B) rho_AB)`. A partial trace over only `A` would leave an operator on `B`, not a scalar.
- §5.2 says Chapter 18 promotes unitary evolution to CPTP maps/quantum channels. Channels are also introduced earlier in Chapter 10/12 according to the reconciliation notes. Consider cross-referencing the first formal channel treatment, then Chapter 18 for noise.
- No-cloning proof is good. Strictly, the inner-product contradiction should allow overlap `0` or `1`; for normalized states, overlap `1` means same ray up to phase if using absolute value, but the chosen example `|0>` and `|+>` makes the contradiction clear enough.

#### `book/part-03-qubits/06-the-qubit.md`

Read end to end. The chapter is compact and mostly effective: basis states, phase, Bloch geometry, single-qubit rotations, and measurement are introduced at the right level.

Review findings:

- This chapter follows the `STYLE.md` status-before-navigation order.
- Opening paragraph says every pure state is a point on the surface of an `ordinary 3-sphere`. Mathematically, the Bloch sphere is a 2-sphere embedded in `R^3`; `3-sphere` means `S^3`, which is the unit sphere in `R^4`. Suggested fix: `ordinary sphere in three-dimensional space` or `unit 2-sphere`.
- §6.10 has a likely gate-order error for circular-basis measurement. Earlier §6.6 defines the computational-to-circular unitary as `V = S H`, so measuring in the circular basis should actively apply `V^dagger = H S^dagger` before computational measurement. The prose says first apply `S^dagger`, then `H`, but the bullet labels the operation as `S^dagger H`, which is the reverse operator order. Suggested fix: write `H S^dagger, then computational` if using algebraic right-to-left operator order.
- §6.6 says `n qubits admit 2^n + 1 mutually unbiased bases`; this is true because `2^n` is a prime power, but a short parenthetical would help readers not generalize incorrectly to arbitrary dimensions.
- Several physical-platform examples in §6.4 are fine as orientation but should stay aligned with Chapter 20 terminology.

#### `book/part-03-qubits/07-multiple-qubits-and-entanglement.md`

Read end to end. The chapter is concise and covers the essential bridge from tensor products to entanglement, Bell states, CHSH, Schmidt decomposition, partial trace, and resource protocols. The teleportation figure is well placed.

Review findings:

- Same global ordering issue as most files: navigation appears before status block, unlike `STYLE.md`.
- §7.1 says the leftmost ket is the most-significant qubit `qubit 0`. Elsewhere, Qiskit reconciliation maps the book's leftmost tensor factor to the highest-numbered Qiskit qubit. This is not a contradiction if `qubit 0` is the book's own abstract label, but it is a high-risk ambiguity. Consider saying `the first/book qubit` rather than `qubit 0`, or explicitly separating book labels from SDK labels.
- §7.4 says entanglement is what most quantum algorithms exploit. This is broadly defensible but can be too strong: some query-model separations use little or no persistent multipartite entanglement depending on formulation. Consider `many` or `most nontrivial multi-qubit algorithms` if precision matters.
- §7.9's Bell/CHSH treatment is clear. If later chapters rely on the exact sign convention for the singlet settings, keep the chosen sign convention consistent.
- §7.12 teleportation corrections are given as `I`, `X`, `Z`, `XZ`; depending on measurement-bit ordering and correction order, some presentations use `ZX`. Since the difference is a global phase for Pauli corrections, this is probably fine, but the bit-to-correction mapping should be explicit in a fuller derivation.


#### `book/part-04-gates-and-circuits/08-quantum-gates.md`

Read end to end. Note: the requested path `book/part-02-formalism/08-quantum-gates.md` does not exist in the current repository; Chapter 8 is currently located at `book/part-04-gates-and-circuits/08-quantum-gates.md`.

The chapter is generally clear and useful as the bridge from states/entanglement into circuit primitives. The progression from Pauli/H/phase/rotation gates through controlled gates, universality, native sets, and synthesis is sensible.

Review findings:

- Same global ordering issue as most files: navigation appears before the status block, unlike the `STYLE.md` status-before-navigation convention followed by some earlier formalism chapters.
- §8.1 says linearity is what makes superposition `processable in parallel` and that applying a gate to a superposition applies it to `every branch at once`. This is common shorthand, but it risks reintroducing the quantum-parallelism misconception that earlier chapters work to avoid. Consider wording it as linear action on all basis components, with interference/measurement constraints doing the computational work.
- §8.3 says a Pauli-X measurement is the same as a Pauli-Z measurement `preceded by H and followed by H`, then says the trailing `H` can usually be dropped. For an actual measurement procedure, the operational statement is simply: apply `H`, then measure in the computational/Z basis. The two-sided `H X H = Z` identity is correct for conjugating observables, but the prose mixes observable conjugation and circuit measurement procedure.
- §8.9 lists `{H, S, CNOT, Toffoli}` as a common universal set. This should be verified or cited. The standard finite universal set in the surrounding discussion is Clifford+T, while `Toffoli + H` is usually discussed for real-amplitude universality and Toffoli is a multi-qubit non-Clifford gate. If this set is retained, explain exactly what universality theorem is being invoked.
- §8.11 says `c = 1` for `good enough` sets such as `{H, T}` via Ross-Selinger. Ross-Selinger-style synthesis is usually stated for Clifford+T single-qubit synthesis and gives logarithmic T-count with strong constants; it is not just a generic Solovay-Kitaev exponent improvement for the bare `{H, T}` set. Tighten the claim or cite the exact theorem intended.
- §8.12 contains explicit 2026 native-gate claims for IBM/Google-style superconducting devices, IonQ/Quantinuum-style trapped ions, neutral atoms, and photonic platforms. These are exactly the kind of perishable claims that should be fact-checked against the dated hardware snapshot before release.
- §8.14's KAK/CNOT-cost summary appears too compressed and likely inaccurate. In the usual canonical-coordinate classification, local gates need zero CNOTs, controlled-unitary-type points need one, a broader lower-dimensional subset needs two, and generic two-qubit unitaries need three; the condition is not simply the surface `c_x = c_y`. The sentence about iSWAP and `sqrt(SWAP)` needing `zero, one, or two CNOTs depending on the native set` also conflates native-gate availability with decomposition into CNOTs. This section needs a careful check against the canonical two-qubit synthesis classification.
- §8.7's multi-controlled-gate cost statement is useful but broad. Costs depend on clean vs dirty ancilla, relative-phase Toffoli allowance, and whether the target gate is arbitrary or has special structure. Consider marking the stated `O(k)`/`O(k^2)` bounds as representative rather than universal.
- The sanity checks are appropriate. In item 3, `R_Y(pi)|0>` equals `|1>` exactly under the matrix convention used here, so the requested global phase is just `1` / zero phase; that is fine, but the exercise may surprise readers expecting a nontrivial phase.

#### `book/part-04-gates-and-circuits/09-quantum-circuits.md`

Read end to end. The chapter is a useful operational bridge from gates to executable circuits: diagrams, ancillae, uncomputation, measurement timing, feedforward, depth, width, routing, and optimization are all introduced in a sensible order.

Review findings:

- Same global ordering issue as most files: navigation appears before the status block, unlike the `STYLE.md` status-before-navigation convention.
- §9.1 says Qiskit puts the least-significant qubit on top `so it lines up with classical bit-string order`. This is too compressed and may confuse readers. Qiskit circuit diagrams usually show qubit 0 at the top, and qubit 0 is often the least-significant bit in integer/statevector indexing, while displayed measurement strings are conventionally written with the most-significant classical bit on the left. This should be aligned with the more careful endianness explanation in Chapter 4.
- §9.4's displayed uncomputation pattern appears to put the inverse in the wrong compositional order. The standard compute-copy-uncompute sequence is `U_compute`, then `U_copy`, then `U_compute^dagger`; in right-to-left function composition that is `U_compute^dagger \circ U_copy \circ U_compute`. The current display reads as if `U_compute^dagger` is applied first.
- §9.7 states `Meas \circ C(U) = C(U) \circ Meas` formally. The deferred-measurement principle is about equality of joint outcome statistics after replacing classical control with coherent quantum control; it is not a literal equality of operations in all contexts. Tighten the statement to avoid overclaiming.
- §9.8 says mid-circuit measurement is `a measurement performed before the circuit ends, with the qubit available for further use afterwards`. That is true for reset/reuse workflows, but not every mid-circuit measurement leaves the same physical qubit reusable, especially on destructive-measurement platforms. The later non-demolition qualification helps, but the opening definition should be less absolute.
- §9.8 includes current hardware support claims for ion traps, superconducting devices, neutral atoms, and photonics. These should be included in the dated hardware fact-check pass because mid-circuit measurement and feedforward support are changing quickly.
- §9.9 says teleportation-based `T` gates in many surface-code schemes use Bell measurement on a magic state followed by a feedforward Pauli correction. For standard `T`/magic-state injection, the adaptive correction is typically a Clifford phase correction such as `S`/`S^dagger`, not merely a Pauli. This should be checked and stated precisely.
- §9.9 describes error correction as measuring syndromes and applying corrections based on the syndrome bits. In many fault-tolerant implementations corrections are tracked in a Pauli frame rather than physically applied each cycle; the text can keep the intuition but should mention frame tracking to avoid a misleading hardware picture.
- §9.10 says Solovay-Kitaev contributes a `log(1/epsilon)` depth factor to many practical algorithms. This is plausible in a fault-tolerant discrete-gate discussion, but for NISQ/native-rotation circuits it is not generally the right cost model. Consider qualifying it as a fault-tolerant compilation effect.
- §9.12 says each routing SWAP costs three CNOTs. That is true for a CNOT-native decomposition, but native SWAP/iSWAP/fSim-style entanglers or pulse-level implementations can change the cost. Since §8.12 emphasized native gate sets, this should be phrased as `on a CNOT-native target`.
- The sanity checks are mostly good. Item 2 says `applies a Toffoli, and measures the ancilla` and asks readers to show data/ancilla entanglement without uncomputation. A single Toffoli with a computational-basis data register may only create classical correlation; to force entanglement, specify a superposed data register such as controls in `|+>|+>`.

#### `book/part-04-gates-and-circuits/10-core-quantum-phenomena.md`

Read end to end. The chapter is pedagogically valuable as a synthesis chapter: it restates superposition, interference, Born probabilities, measurement disturbance, no-go theorems, contextuality, decoherence, Lindblad dynamics, channels, and Kraus/Stinespring representations from an operational computing viewpoint.

Review findings:

- Same global ordering issue as most files: navigation appears before the status block, unlike the `STYLE.md` status-before-navigation convention.
- The opening `How to read` says open-system machinery becomes load-bearing in `Parts 6 (error correction) and 7 (hardware)`. In the current manuscript, error correction is Part 8 and hardware is Part 9. This is a concrete part-numbering typo.
- §10.1 repeats the `gate evaluates on every branch at once` phrasing. The chapter immediately gives a better interference-based explanation, but the `branch` wording should be kept consistent with the book's earlier warnings against naive quantum parallelism.
- §10.4 says a sharp `Z` value `forces` `Delta X = Delta Y = 1`. The statement is true for Pauli `Z` eigenstates, but it does not follow directly from the Robertson inequality as written; with `Delta Z = 0`, the product bound is trivial unless the state-specific commutator expectation is handled carefully. Consider wording this as an explicit calculation for `|0>` and `|1>`.
- §10.6 calls `{M_a}` `POVM elements`, but then uses them as measurement/Kraus operators satisfying `sum_a M_a^dagger M_a = I`. POVM effects would usually be `E_a = M_a^dagger M_a`. Rename them to measurement operators/Kraus operators, or rewrite the proof using POVM effects.
- §10.6's no-signalling proof uses cyclicity of the partial trace. This can be made correct, but cyclicity under a partial trace only applies to operators acting on the traced subsystem. A short clarification would prevent readers from overgeneralizing cyclic trace rules.
- §10.8 says frequent syndrome extraction in stabiliser codes projects the code `back onto the code space`. Syndrome measurement projects onto syndrome subspaces; returning to the code space requires correction or Pauli-frame tracking. The Zeno analogy is useful, but this sentence should be softened.
- §10.11's Lindblad depolarising example uses jump operators `sqrt(p/4) sigma_k`. In a continuous-time master equation, the coefficient should be a rate such as `gamma`, not the channel probability `p` used in §10.12. As written it mixes discrete channel probability and continuous-time generator notation.
- §10.12 defines the depolarising channel as `E(rho) = (1 - p)rho + p I/2`, then says equivalently each Pauli error `X,Y,Z` occurs with probability `p/3`. Those are not equivalent with the same parameter `p`. The Pauli-error form with total error probability `p` maps the Bloch vector by `1 - 4p/3`; the replacement-by-maximally-mixed form maps it by `1 - p`. To match the latter, each nontrivial Pauli would occur with probability `p/4`, not `p/3`.
- §10.12's phase-damping entry says off-diagonals shrink by `sqrt(1 - p)` and that this is operationally indistinguishable from random `Z` rotations averaged uniformly. A uniformly random phase would fully dephase. If the intended channel is phase damping with a specific parameterization, give Kraus operators or a random-`Z` model with the matching shrink factor.
- §10.14 says Part 6 opens the algorithmic programme; that appears consistent with the current structure, but this bridge should be rechecked after fixing the earlier Part 6/7 typo.
- Sanity check 5 asks readers to start from the Kraus form of the depolarising channel and derive `r -> (1 - p) r`, but §10.12 did not give Kraus operators for the depolarising channel and also has the parameterization mismatch above. This exercise should be updated together with the depolarising-channel definition.

#### `book/part-05-measurement-and-information/11-measurement-theory.md`

Read end to end. The chapter is strong in scope and sequencing: it expands the projective postulate into POVMs, Naimark dilation, tomography, shadows, state discrimination, Pauli-basis estimation, parameter shift, and readout-error mitigation. The practical emphasis is useful.

Review findings:

- Same global ordering issue as most files: navigation appears before the status block, unlike the `STYLE.md` status-before-navigation convention.
- §11.1 says a non-demolition measurement on a single qubit can return a result without collapsing the state to a basis vector, leaving residual coherence. This needs qualification. A non-demolition measurement of a nondegenerate single-qubit observable still projects to that observable's eigenstate; residual coherence is possible within degenerate subspaces or for weak/generalized measurements, not for an ideal projective single-qubit `Z` measurement.
- §11.4 says process tomography characterises an unknown channel by `preparing each of 4^n basis states`. The Pauli operator basis has `4^n` elements, but they are not all physical states. Process tomography uses an informationally complete set of `4^n` input states, then reconstructs a process representation. Reword to avoid calling Pauli basis operators preparable states.
- §11.5 says shadows can estimate observables in a pre-declared class, then says the shots can be queried for many different observables `not just the ones declared in advance`. The samples can be reused, but the stated high-probability sample-complexity guarantee depends on the set of queried observables. Add a caveat for adaptive/post-hoc querying.
- §11.6's unambiguous-discrimination paragraph says unambiguous discrimination is impossible for non-orthogonal mixed states whose supports overlap. The mixed-state condition is more subtle: unambiguous discrimination is possible only when each state has support not fully contained in the other's support. If this paragraph is meant as a generic warning, qualify it; if it is meant as a theorem, state the support condition precisely.
- §11.7 repeats the likely Y-basis rotation order issue seen earlier. To measure `Y` with computational-basis hardware, the usual active circuit is `S^dagger` followed by `H`, i.e. total unitary `H S^dagger` in right-to-left algebraic notation. The text says `S^dagger H` for `Y`, which is the reverse if interpreted algebraically.
- §11.7 says use identity for `Z`, `prepared then discarded for I` in Pauli-string measurement. For an identity factor, one normally does not need to prepare or discard anything for that term; if the qubit is present because of the larger circuit, its measurement result is ignored/marginalized. The current wording is misleading.
- §11.7 says joint measurability of two Paulis in a single shot requires commutation and tensor-product joint diagonalization, which is automatic iff the Paulis commute qubit-wise. This is correct for the local-basis-rotation workflow, but commuting non-qubit-wise Paulis can be jointly measured using entangling Clifford basis changes. Clarify that the `iff` applies to single-qubit basis rotations only.
- §11.7's readout-error mitigation says tensor-product readout models reduce the cost to `O(2 n)`. This should be written as `O(2n)` or `O(n)` depending on whether the intended cost is calibration circuits. Also distinguish calibration cost from applying a factorized inverse to a full `2^n` outcome distribution.
- The sanity checks are well targeted. Item 1's trine POVM exercise is good; adding the Bloch-vector identity `sum_k n_k = 0` and `sum_k n_k n_k^T = (3/2) I` would make the verification less mysterious.

#### `book/part-05-measurement-and-information/12-quantum-information-theory.md`

Read end to end. This is one of the more polished chapters structurally: it follows the status-before-navigation convention, builds cleanly from Shannon/von Neumann entropy to distinguishability, capacities, LOCC, and algorithmic consequences, and repeatedly gives operational meanings rather than only formulas.

Review findings:

- This chapter follows the `STYLE.md` status-before-navigation order, unlike most nearby chapters.
- The `How to read` note says §§12.8-12.10 preview material expanded in `Part 6 and Part 8`. No-cloning/capacities/error correction fit Part 8, but Part 6 is algorithms; if channel capacities are mainly Chapter 18/Part 8, this cross-part preview should be checked.
- §12.4's state-merging explanation appears to conflate entanglement cost with quantum communication cost. In standard state merging, the conditional entropy `S(A|B)` is the entanglement cost/gain under LOCC with classical communication, not simply the number of qubits Alice must send when positive. The operational interpretation should be restated carefully: positive `S(A|B)` means entanglement consumption; negative `S(A|B)` means entanglement generation.
- §12.7 says for the Fuchs-van de Graaf inequalities, `Either side becomes an equality on pure states`. For pure states, the upper bound `D = sqrt(1 - F^2)` is tight under the unsquared fidelity convention used here. The lower bound `1 - F <= D` is generally not equality except in special cases such as identical or orthogonal states.
- §12.8 says no-cloning/no-deleting imply quantum information under closed-system evolution is `conserved`: it can be neither duplicated nor erased. This is a useful intuition, but it should not be overread as a formal conservation law. The formal statement is reversibility/unitarity plus no-cloning/no-deleting for unknown states; information can still be moved into correlations or an environment.
- §12.9 describes the source as emitting copies `rho^{otimes n}` and the reconstruction as having high fidelity to `rho^{otimes n}`. Schumacher compression is usually stated for an ensemble/purification source with high entanglement fidelity or average fidelity, not merely reproducing a known mixed density operator. The current wording is acceptable for intuition but should be sharpened if this chapter is meant to be mathematically precise.
- §12.10's capacity formulas are high-level and broadly right, but the notation `max_rho I_c(rho, N^{otimes n})` should make clear that `rho` ranges over input states to `N^{otimes n}`. Otherwise it reads like a single-use input state.
- §12.13 says no-cloning is why quantum algorithms cannot use the classical trick of running the same subroutine twice on the same input to amplify confidence at zero extra cost. This is true for an unknown quantum input state, but not for ordinary classical problem instances, which can be copied and re-prepared. Qualify the statement so it does not sound like no-cloning forbids repeated algorithm runs.
- Sanity check 3 asks readers to verify the Fuchs-van de Graaf inequalities for two pure states `with equality on the pure-state side`. This should specify the upper inequality; the lower inequality is not tight for `|0>` and `|+>`.
- Sanity check 5 says subadditivity is `violated only as equality (never strictly) for |Phi+>`. That is incorrect wording. For a Bell state, `S(AB)=0` and `S(A)+S(B)=2`, so subadditivity holds strictly; it is not equality and is not violated.

#### `book/part-06-algorithms/13-quantum-algorithms-mindset.md`

Read end to end. The chapter is effective as a conceptual bridge into algorithms: it pushes against the parallel-evaluation myth, centers interference and phase kickback, and introduces query-model, transform-domain, hidden-structure, success-probability, and complexity-cost vocabulary at the right level.

Review findings:

- Same global ordering issue as most files: navigation appears before the status block, unlike the `STYLE.md` status-before-navigation convention.
- §13.1 says `No black-box query yields more information than a single classical query would have.` This is directionally useful, but too absolute as phrased: one quantum query can produce global information about a promised oracle that one classical point query cannot, as Deutsch-Jozsa and Bernstein-Vazirani demonstrate. The safer point is that measurement does not expose all branch values and the advantage comes from interference across amplitudes.
- §13.3 uses the phrase `This caveat is the seam between query-model and uniform-circuit-model results`. `Seam` is metaphorical and may be less clear than a direct phrase like `the distinction between`.
- §13.4's QFT convention uses `e^{-2 pi i x y / N}`. This is fine if the rest of the book uses the same sign for the forward QFT, but it should be checked against Chapter 4 and Chapter 14 so inverse-QFT/phase-estimation formulas do not silently flip signs.
- §13.4 correctly warns that controlled `U^{2^k}` is cheap in Shor-style modular exponentiation but not for a black-box `U`. This caveat is important; keep it explicit in Chapter 14's phase-estimation section too.
- §13.7 says `P subseteq BPP subseteq BQP subseteq PSPACE` and then says each containment is conjectured strict. That is not the standard belief for `P` versus `BPP`; most complexity theorists conjecture `BPP = P` under derandomization assumptions. Rephrase as `some containments are conjectured strict` or state the common belief separately.
- §13.7 says exponential simulation speedups over the best known classical algorithms occur for many natural physical problems. That is plausible as a best-known-algorithm comparison, but it should not read as a proved complexity separation unless the problem family and assumptions are stated.
- §13.7 says for quadratic speedups one must compare fault-tolerant quantum running time to `uncorrected` classical running time. The intended point is overhead-aware end-to-end comparison, but `uncorrected classical` is odd because classical machines also use reliability mechanisms. Consider `best practical classical implementation` or `classical implementation including its real hardware costs`.
- The sanity checks are appropriate and reinforce the chapter's conceptual aims.

#### `book/part-06-algorithms/14-foundational-algorithms.md`

Read end to end. The chapter gives a compact tour of the standard primitives and mostly does a good job preserving the algorithmic through-line from phase kickback to QFT/phase estimation and amplitude amplification.

Review findings:

- Same global ordering issue as most files: navigation appears before the status block, unlike the `STYLE.md` status-before-navigation convention.
- §14.2 includes a runnable Qiskit snippet using `StatevectorSampler`. Verify this against the exact Qiskit version targeted by the project; sampler APIs and result containers have changed across recent Qiskit releases.
- §14.4 says Simon's algorithm is the first exponential quantum speedup over any classical algorithm. This should explicitly say `in the oracle/query model`; otherwise it can be misread as an unconditional time-complexity separation.
- §14.5 defines the QFT with a negative exponent, then describes the circuit using controlled rotations `R_k = diag(1, e^{+2 pi i / 2^k})`. For the negative-exponent convention, the controlled phase angles should be conjugated unless the circuit is intentionally the inverse relative to common SDK notation. This is a high-priority sign-convention check.
- The QFT sign issue also appears in sanity check 3: for the negative-exponent definition, `QFT_4|1>` should have amplitudes `1/2, -i/2, -1/2, i/2`, not `1/2, i/2, -1/2, -i/2`. The listed answer matches the positive-exponent convention.
- §14.6's phase-estimation explanation is internally plausible under the negative-exponent convention: the controlled powers create a positive-phase state and the forward negative-sign QFT reads it out. But this depends on §14.5's circuit and sanity-check signs being corrected consistently.
- §14.6 says if `U` is a simple gate, `U^{2^k}` can be implemented in `O(1)` gates by squaring. This is true for special parameterized/structured gates, but not for an arbitrary simple gate presentation. Consider saying `when the powered family has a closed-form efficient implementation`, with modular exponentiation as the main example.
- §14.7 defines `G = (2|psi><psi| - I)(2P_good - I)` and then says `P_good` is implemented by phase-flipping marked states. The reflection `2P_good - I` leaves good states positive and flips bad states; the usual marked-state phase oracle is `I - 2P_good`. The two differ by a global sign at the reflection level, but the prose and formula should use the same convention.
- §14.8 defines amplitude estimation as estimating the marked-amplitude `a = sin(theta)`. Standard amplitude-estimation notation usually estimates the success probability `a = sin^2(theta)`. Because the Monte-Carlo comparison is about estimating a probability, this should be reparameterized or explicitly explained.
- §14.9's HSP examples include `G = a function over Z^n encoding integer factoring: factoring reduces here`. This line is unclear and likely redundant: factoring reduces to order-finding, which is already listed as the `Z_N`/period-finding example. Consider removing or replacing it with a precise hidden-subgroup formulation.
- §14.9's dihedral/lattice sentence is too broad as written. Dihedral HSP is related to certain lattice problems and Kuperberg gives subexponential algorithms, but `CVP/SVP lineage` needs precision about which approximation regimes and reductions are meant.
- §14.9 says every known exponential quantum speedup outside Hamiltonian simulation either fits the abelian HSP template or factors through it. This is too sweeping unless heavily qualified; oracle speedups such as glued-trees quantum walks and forrelation-type separations do not naturally fit that statement.

#### `book/part-06-algorithms/15-landmark-quantum-algorithms.md`

Read end to end. The chapter is well chosen as a survey of named algorithms and near-term algorithm families, and it repeatedly warns about the assumptions behind headline speedups. The main issues are precision of resource estimates, a few overbroad claims, and one placeholder cross-reference.

Review findings:

- Same global ordering issue as most files: navigation appears before the status block, unlike the `STYLE.md` status-before-navigation convention.
- §15.1 gives Grover's iteration count as `floor(pi/(4 theta))`. The usual near-optimal count is the nearest integer to `pi/(4 theta) - 1/2`; the simpler formula is asymptotically fine but can overshoot for small instances. If the section is giving a success bound, use the standard rounded expression.
- §15.1 says the success probability is `>= 1 - M/N`. This bound depends on the exact iteration-choice convention; check it together with the iteration-count formula.
- §15.1 includes runnable Qiskit code using `StatevectorSampler`; as with Chapter 14, verify against the exact Qiskit version targeted by the examples.
- §15.2 inherits the QFT sign-convention risk from Chapter 14. The order-finding explanation should be rechecked after the Chapter 14 QFT sign/circuit/sanity-check issue is fixed.
- §15.2 says NIST finalised `CRYSTALS-Kyber` and `CRYSTALS-Dilithium` in 2024. This should be updated to the standardized names as appropriate (`ML-KEM`/`ML-DSA`, with Kyber/Dilithium as the underlying families) and fact-checked before release.
- §15.3's RSA-2048 resource estimate is high priority for external verification. The line `~3 x 10^6 logical qubits` appears inconsistent with `~10 million physical qubits at surface-code distance d approx 27`; millions of logical qubits at that distance would imply far more than ten million physical qubits. This may be a unit/category error.
- §15.3's other specific resource claims (`~10 million physical qubits`, `~10 hours`, `~7 x 10^9 Toffoli gates`, current `10^2-10^3` physical qubits and `10^-3` errors) are perishable and should be aligned with Appendix F and cited estimates.
- §15.3 says the post-quantum migration question is `when, not whether`. This is defensible as risk framing, but it reads like a forecast. Consider making the threat-model assumption explicit: sufficiently large fault-tolerant quantum computers would break RSA/DLP, but timelines remain uncertain.
- §15.4 says ECDLP is roughly half the qubits and gate count of RSA factoring for comparable security. This is a concrete comparative resource claim and needs a citation or softening.
- §15.5 describes HHL as `eigen-decomposing A` via phase estimation. It is really a coherent spectral transformation, not a classical eigendecomposition; the distinction matters because HHL never learns all eigenvectors/eigenvalues.
- §15.5 says dependence on `kappa` is unavoidable and that `kappa = Omega(N)` kills the speedup. The qualitative warning is right, but modern linear-system algorithms have improved condition-number dependence compared with original HHL. Consider noting that the displayed complexity is the original/simple HHL scaling.
- §15.5 says most proposed HHL applications have been dequantised by Tang and follow-ups. This is a useful warning but broad; qualify it as applying to many data-loading/QRAM-based classical-data applications under comparable sampling/query assumptions.
- §15.6 says the glued-trees quantum walk gives polynomial versus exponential separation. This should explicitly say it is an oracle/query-model graph-access separation, not a conventional explicit-input time-complexity separation.
- §15.6 says Szegedy quantization gives a quadratic speedup in mixing time. The standard quantum-walk speedups are usually phrased in terms of hitting/search or spectral-gap dependence; `mixing time` as written may be too broad.
- §15.6 contains a placeholder cross-reference: `qubitization, §16.x`. Replace this with the actual Chapter 16 section number once Chapter 16 is stable.
- §15.8's VQE shot-count estimates for a `50-orbital active space` and `days of real-time device usage` are concrete 2026-era claims. They should be sourced or softened because shot budgets depend heavily on Hamiltonian, ansatz, grouping, target precision, device repetition rate, and mitigation strategy.
- §15.9 says QAOA converges to the exact ground state as `p -> infinity` via the adiabatic theorem. This is true with appropriate parameter schedules, but it should not imply arbitrary optimized finite-p QAOA automatically has an efficient path to the optimum.
- §15.10's `2026 picture` of QML is appropriately skeptical, but it contains current-state claims that should be part of the dated fact-check pass.
- Sanity check 5 asks the reader to look up a current RSA-2048 resource estimate. That is a good exercise for an advanced reader, but if this book is meant to be self-contained offline, provide at least one named baseline estimate in the text or appendix.
